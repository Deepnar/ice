# ICE Post-Paper Roadmap

> Living checklist of everything queued after the paper. Distilled from [rough_post_paper_work.md](rough_post_paper_work.md) (raw notes — keep that file as the source of nuance), the experiment reports in `experiments/*/results*`, and the paper's limitations section.
>
> **How to use this file:**
> - **Work it in order** (Phase 0 → Track A → B → …), respecting dependencies. The ordering is deliberate.
> - **Build up, never replace.** Each item must be implemented so that *later* items build on top of it, not tear it out. Concrete example: the decay / background-worker scheduling fixes (C7) are later absorbed by the agentic Memory Maintenance Agent (Track D) — so do C7 with clean, composable worker interfaces the agent can later orchestrate, not a throwaway patch. When implementing any item, check what downstream items depend on it and design for that end-state.
> - **No first versions — build the robust, considered version.** "Perfect" is impossible, but every feature is implemented as the durable, thought-through version, not a quick MVP to be redone later: handle the edge cases, use the proper algorithm/structure (not a toy stand-in), and design for the end-state. If the proper version is too big for one pass, split it into robust sub-items — never ship a knowingly-throwaway version.
> - **Look ahead before building (mandatory pre-step).** Before implementing ANY item, scan the rest of the roadmap for later items in the **same realm/subsystem**. If a later item will rework the same area: (a) design the current implementation to be forward-compatible with where that item is heading (pick the primitive the future item needs — e.g. A5 filters on a *batch-id set* because C6's cross-chat/session/@-mention scoping all resolve to that same set); or (b) if they genuinely conflict and can't be reconciled, decide explicitly and record it — do the later item first, or note the exact seam that will need changing. Never implement in a way a *known* future item will have to tear out. State the look-ahead result in the item's completion note.
> - **Propagate on completion — refresh downstream stale references (mandatory post-step).** The mirror of look-ahead. After finishing an item, scan the still-unchecked items for any that describe the *old* behavior of what you just changed, and update them to the new reality (note the new dependency / what's now already done). A completed feature that leaves later items describing the pre-change world will silently mislead a future session (e.g. after A6, item D1 no longer "replaces the pipeline"; after A7, F3's graph view must render typed nodes + backlinks + negations). Only mark an item `[x]` after this pass.
> - **Earn the checkmark.** Only `[x]` an item after its full *original* scope is implemented **and** behaviorally validated (a real run/test, not just a syntax check) — audit against the entry text, not memory of it. If part is genuinely deferred, say so explicitly in the note rather than checking it off.
> - Each entry is *intent + rationale*, **not a spec**. When an item's turn comes, the concrete design gets discussed in-session first; only then is it implemented.
> - Check off `[x]` when done. On completion, update [ICE_Architecture.md](ICE_Architecture.md): new systems get a new section, reworked systems get their existing section edited.
> - Phase 0 decisions may kill, reshape, or re-home items below (some conversational features may move to the Coding side). That is expected.
> - **⚑ REMEMBER THE END-STATE (full statement in the Track F preamble — easy to forget, don't):** `./ice`, `./stop_ice`, and `setup.sh` are **dev scaffolding, destined for replacement** by one packaged frontend+backend app that boots the whole stack itself — and **ICE-as-MCP's SEPARATE headless boot story SHIPPED with E7 (2026-07-17)**: `ice-mcp` attaches to postgres if live, else boots it via docker, then `create_core()` with the runtime-lease/standby arbitration, and lingers on shutdown (`./stop_ice` stays the full-stop path). Any item that touches startup, services, or the stack (`./ice`, workers, docker, model servers) must be written and built knowing both destinations (packaged app + headless MCP boot).
>
> > **Corrections (verified in code, 2026-07) — the paper's limitations section repeats two claims that are false in the actual code; fix before arXiv:**
> 1. **NER was loaded, not regex-fallback.** [ner_utils.py](../src/retrieval/ner_utils.py) loads `models/ner/ner_model.pt` (present on disk) and only regex-falls-back if that file is missing. NER was active during the experiments (empirically ~95% recall). Codex's weak numbers come from other handicaps, **not** entity detection.
> 2. **No "corroboration trap" starving retrieval.** Codex retrieval filters only on `valid_until == None` (time-validity) in `_traverse_graph`, MERA, and context_payload regeneration — **none filter `confidence == 'active'`**. So `pending` (single-extraction) edges *are* traversed and injected; `confidence=='active'` only gated a coarse 1.5× score boost. The claim that pending edges never contribute to retrieval is false. The real Codex weaknesses are chunk size (A1), entity-vs-relation retrieval (A4), leg under-representation (A10), and an inert confidence signal with no reinforcement (A3, now fixed).

**Why-tags used below:** `(planned)` designed but never implemented · `(rework)` exists but the current version is bad/underperformed in experiments · `(bug)` broken behavior found in audit/experiments · `(new)` new direction from post-paper thinking · `(open)` limitation with no settled solution yet.

---

## ⚡ HOW TO EXECUTE THIS ROADMAP (post-Fable implementation flow — S1 close-out, 2026-07-10)

> Written by the S1 spec session. Every design-heavy item has a **decision-complete spec in [docs/specs/](specs/)** — implementation sessions follow THIS order, read the item's spec **and its `Assumes decided specs:` chain** first, and obey [specs/README.md](specs/README.md) rules 11–12 (USER-REQUIRED steps are first-class; the divergence protocol is mandatory).

**Current position (2026-07-19, late session): Phase 7 COMPLETE (G23+C17 shipped — data longevity + the 1024 cutover).** G23 backup `78537c9`; core + migration `d4d0a79` (migration `b6e2f9a41c73` = the new alembic head): store_meta fail-loud guard in `create_core()`, portable JSONL export/import, shared backup module, introspective resumable re-embed runner, ONE shared native-width embedder (five truncate_dim copies died), slice384 staging for classifier/NER (bit-identical, proven). **The live store was archived (17 MB `backups/ice_backup_20260719_115426_paper-era-pre-1024-wipe.tar.gz`; Exp2's mature store was already preserved at `~/ice_exp2_mature_snapshot.sql`, 47 MB) and — BY USER DECISION — EMPTIED rather than re-embedded: ICE now runs on an empty store at 1024.** ⚠ **No project is registered anymore** — the wipe removed the E-core session's 'ice' registration and the user declined re-registering ("i never registered it"); `scripts/register_project.py` when wanted. Validated: longevity 26/26 (all 8 spec checks incl. MRL bit-identity + scratch-DB restore), regressions all green (c10_c11 56/56, c4_c9 28/28, services 48/48, mcp 21/21, timescope 61/61, coding_core 46/46, maint_agent 43/43, maint_runtime 49/49, reconcile 16/16, session_scoping 13/13, retrieval 3/3 — house-rewritten, its TRUNCATE died — smoke 77). Earlier: Phase 6 C4/C9 `2c33258` + C10/C11 `e3f24cb` + E11 `548b433`; Phase 5 E-core `fe8df63`; Phase 4 D1/D2 `7cc02b0`; Phase 3 E0+E7 `f43feeb`; Phase 2 Track T; Phase 1 C7. Next phase: **F10/F14** ([F10_F14_import.md](specs/F10_F14_import.md)) — **USER-REQUIRED opens it: export your chat histories from the providers (ChatGPT: Settings → Data controls → Export; Claude: Settings → Privacy → Export data; ~10 min total) and pick a decay policy when asked; machine stays on for big imports.** Standing: **do NOT remind the user about MCP registration** (deferred by their decision until the memory is worth mounting — and with the store freshly emptied, it is not; revisit only when they raise it).

> **📌 DOCS ARE IN SYNC as of 2026-07-19 (end of the G23+C17 session — supersedes the C10/C11 note below, which remains true underneath).** Every doc this session touched was updated in-session: (a) **this roadmap** — G23 + C17 checked off with completion notes (backup `78537c9`, core `d4d0a79`, migration `b6e2f9a41c73`; longevity 26/26 + regression sweep); position/Next point at F10/F14 with its USER-REQUIRED provider-exports step surfaced; the ⚠ no-project-registered reality recorded (wipe + user declined re-register); propagation sweep updated A9 (slice384 staging now LIVE), B1 (what its retrain retires), the F-ledger's G23 line (buttons now wrap shipped scripts), F10 (sibling-import distinction + empty-store note), the USER-REQUIRED table row 7 (done), and the BRUTAL_ASSESSMENT blurb (⚡⚡ item CLOSED). (b) **[ICE_Architecture.md](ICE_Architecture.md)** — §2.1 (slice384 MRL prefix, bit-identity), §3 intro + every schema mention at Vector(1024), §4.5 (A4 re-norm deleted), §4.6 (NER slice), §8.10 (fine-tune slicing), §10.2 (1024 + first-ever HNSW indexes + store_meta pointer), §10.6 (embedding-identity settings), and a new **§10.7** (data longevity: guard/backup/export-import/re-embed runner + the cutover history). (c) **[docs/specs/G23_C17_data_longevity.md](specs/G23_C17_data_longevity.md)** — the [rev 2026-07-19] re-grounding note (12 refinements: nine columns, per-writer source-text verification, husk+static skip rule, six truncate_dim sites ⇒ embedder consolidation, slice384 = raw prefix bit-identity, no live vector indexes, ice_reembed CLI, single-revision seeding semantics, the USER wipe decision, 26-table export list, backup-includes-.env, check-3 adaptation) was recorded BEFORE coding (rule 12). (d) **[CLEANUP.md](CLEANUP.md)** — test_retrieval TRUNCATE removal + MiniLM swap, stub-width updates, gitignore additions logged. (e) **[docs/specs/FINAL_experiments.md](specs/FINAL_experiments.md)** — its G23 look-ahead line now points at the shipped `backup.snapshot_db()`. **If you (next session) touch a subsystem, keep this discipline: update its ICE_Architecture.md section + any stale spec/roadmap references in the same session, then note it here.**
>
> **📌 (previous) DOCS-IN-SYNC note from the C10/C11 session, 2026-07-19 (remains true underneath).** Every doc this session touched was updated in-session: (a) **this roadmap** — C10 and C11 checked off with completion notes (both commit `e3f24cb`, 56/56 + regressions: services 48/48, mcp 21/21, c4_c9 28/28, session_scoping 13/13, smoke 72); position/Next point at G23+C17 with its USER-REQUIRED backup step surfaced; propagation sweep updated B6 (branch deletion reuses the C10 cascade), G25 (the manifest caveat now ships and names it), F16 (the /deep-search surface extends the live C11 parser), F2 (forget_request + stale in the queue it renders), and the F-ledger's C11 line (parser SHIPPED; F owes discoverability only). (b) **[ICE_Architecture.md](ICE_Architecture.md)** — §1.1 pre-flight step (0) command short-circuit; §10.1 DELETE endpoint row + chat-completions row note; §11 intro (adapter #3 live); §11.1 `conversations.py` + the review dispatch's `forget_request` arm and pending/approved/rejected/resolved/**stale** vocabulary; §11.2 `ice_control` 18→20 actions; new **§11.4** (deletion cascade + chat commands, mechanics and validation). (c) **[docs/specs/C10_C11_deletion_chat_control.md](specs/C10_C11_deletion_chat_control.md)** — the [rev 2026-07-19] re-grounding note (15 refinements: post-grounding cascade tables, external-evidence corroboration, entity-husk mechanics, `_expire_edge` source kwarg, T-amendment already implemented, stale detection incl. forget_request, both-FK empty-cluster rule, in-process mid-gen check, try_handle scope param, skip-storage + SSE shape, confirm TTL, forget_request naming + PgVector bind, /scope passthrough, /bookmark reality, sync-def adapters) was recorded BEFORE coding (rule 12). (d) **[CLEANUP.md](CLEANUP.md)** — models.py review-status comment fixed (was already lying about `resolved`), no moves/deletes. **If you (next session) touch a subsystem, keep this discipline: update its ICE_Architecture.md section + any stale spec/roadmap references in the same session, then note it here.**
>
> **📌 (previous) DOCS-IN-SYNC note from the E11 + C4/C9 session, 2026-07-19 (remains true underneath).** Every doc this session touched was updated in-session: (a) **this roadmap** — E11, C4, C9, G14 checked off with completion notes (E11 `548b433` 16/16; C4/C9 `2c33258` 28/28 + regressions); position/Next point at C10/C11; propagation sweep updated the T-track seam line (C4's era-digest source rows are live). (b) **[ICE_Architecture.md](ICE_Architecture.md)** — E11 landed in §12.3 (read-path freshener) + §11.1/§11.2 in its own docs commit `cd5c1b4`; the C4/C9 pass updated §3.5 (three-tier slots), §5.2/§5.4 (whitelist dead, floor, the PgVector dead-leg root-cause), §6.1 (batch-summary leg union), §7.1/§7.2 (summary block + tier order), §8.1 (quartet + `conversation_summary 7200`), §8.6/§8.9 (proposed_by), new §8.13 (the conversation-summary worker), §11.1 (slots tier dict + review re-route), §11.2 (`ice_slots` tiers), §12.5 (project-tier slot inheritance). (c) **[docs/specs/C4_C9_memory_quality.md](specs/C4_C9_memory_quality.md)** — the [rev 2026-07-19] re-grounding note (10 refinements: slots service location, missing uniqueness reality, leg position + E1/T3 logic, extraction-side audit result, burst quartet + cadence, window-condition source, assembly block order, cross-half exclusions, D7 payload/proposer mechanics, incognito summary semantics) was recorded BEFORE coding (rule 12). (d) **[CLEANUP.md](CLEANUP.md)** — the PgVector dead-leg fixes (procedural + rag) and the test_services review-section contract update logged; no moves/deletes. **If you (next session) touch a subsystem, keep this discipline: update its ICE_Architecture.md section + any stale spec/roadmap references in the same session, then note it here.**
>
> **📌 (previous) DOCS-IN-SYNC note from the E-coding-core session, 2026-07-18 (remains true underneath).** Every doc this session touched was updated in-session: (a) **this roadmap** — E1/E1b/E2/E3/E4/E6/E8/E9 checked off with completion notes (commit `fe8df63`, 46/46 + regressions); position/Next point at C4/C9; propagation sweep updated A7.4 (the code graph it runs over now exists), C6 (the episodic `conversation_ids` IN-filter shipped early as D11's `_conv_scope_filter` — C6-F only populates scope), C9 (the `projects` table + `procedural_memory.project_id` it hangs off are live), and E10 (now waits ONLY on C12). (b) **[ICE_Architecture.md](ICE_Architecture.md)** — new **§12 Coding ICE** (projects, code graph, reconcile-on-commit, decisions, facts, session-start, routing); §8.1 cadences gained `project_poll 600`; §8.9's detector list gained `stale_work`; §11.1 notes the new `projects.py` service + scoping's `project` kwarg; §11.2's `ice_where`/`ice_recent`/`ice_control` descriptions updated for the engine swaps. (c) **[docs/specs/E_coding_core.md](specs/E_coding_core.md)** — the [rev 2026-07-17] re-grounding note (13 refinements: worklist/commit seams concrete, no edge-properties column ⇒ extraction_confidence encoding, hook transport, deterministic static batch id, visibility mechanics, conversation_ids leg filter, preflight session-start trigger, schema pins, attach path, husk+derived detector filter, parse-error handling, registration surface) was recorded BEFORE coding (rule 12). (d) **[CLEANUP.md](CLEANUP.md)** — one stale-assertion update logged (no moves/deletes this session). **If you (next session) touch a subsystem, keep this discipline: update its ICE_Architecture.md section + any stale spec/roadmap references in the same session, then note it here.**

> **📌 (previous) DOCS-IN-SYNC note from the D1/D2 session, 2026-07-17.** Every doc the D1/D2 session touched was updated in-session: (a) **this roadmap** — D1 and D2 checked off with completion notes (commit `7cc02b0`, 43/43 + regressions); position/Next point at E-coding core; propagation sweep updated G17 (source vocabulary live: `mcp_edit`/`manual_edit`/`maintenance_agent`; sentinel dropped from its list) and D3 (the `agent_action`/`agent_run_summary` events now exist — only F5's SSE promotion remains). (b) **[ICE_Architecture.md](ICE_Architecture.md)** — **§8.9 is now the Memory Maintenance Agent** (detectors, tiers, caps, `merge_entities`, husk semantics; the Sentinel Monitor section is gone); §8.1's cadence line swaps `sentinel_monitor 1800` for `maintenance_agent 43200` and the session-end burst is a trio; §11.1's `review.py` paragraph no longer says "stub" — approval performs the real D5 merge, and `status="resolved"` is documented. (c) **[docs/specs/D1_D2_maintenance_agent.md](specs/D1_D2_maintenance_agent.md)** — the [rev 2026-07-17b] re-grounding note (six refinements: `source=` kwarg, default-decider sentinel semantics, `resolved` status, husk-rename necessity, detector-2 port deviations, sentinel residue list) was recorded BEFORE coding (rule 12). (d) **[CLEANUP.md](CLEANUP.md)** — sentinel deletions + seed-script move logged. **If you (next session) touch a subsystem, keep this discipline: update its ICE_Architecture.md section + any stale spec/roadmap references in the same session, then note it here.**

> **📌 (previous) DOCS-IN-SYNC note from the E0+E7 session, 2026-07-17.** Every doc this session touched was updated in the same session, so a new session can trust them: (a) **this roadmap** — E0 and E7 checked off with completion notes; the top position/Next lines point at D1/D2; propagation sweep updated the ⚑ end-state note + Track-F's headless-boot addendum (the E7 boot story is now DECIDED AND SHIPPED: attach-else-boot + lease/standby + linger) and G20 (E0 preserved the router surface byte-identically, so its sweep items 2/3 remain open, re-homed to C6/F5). (b) **[ICE_Architecture.md](ICE_Architecture.md)** — new **§11 Service Layer & ICE-as-MCP** (services, tool surface, boot/lease/standby, validation); §8 intro + §8.1 gained the runtime-lease/standby paragraph; §10.1 now describes the routers as thin adapters over `src/services/` with the parity-harness guarantee. (c) **[docs/specs/E0_E7_services_mcp.md](specs/E0_E7_services_mcp.md)** — rev notes 1–10 recorded BEFORE coding (rule 12): D2(a) had already shipped with C7; pyproject needed a build-system for the `ice-mcp` entry point; "the C7 lease" was made concrete as the `runtime_lease` ledger pseudo-row; defer = STANDBY runtime (not no-runtime — two silent-data-loss states otherwise); context_for always orchestrates on explicit pull; ice_remember's bookmark mechanics; the codex_reconciliation approve action; entity_edit as the birthplace of manual description editing; review.reject added with no new REST endpoint. (d) **[docs/specs/D1_D2_maintenance_agent.md](specs/D1_D2_maintenance_agent.md)** — rev note added: the D6 approve-dispatch now lives in `src/services/review.py` (E0 lifted it, both agent arms pre-wired; `codex_ops.merge_entities` is the loud stub D1 replaces). (e) **[CLEANUP.md](CLEANUP.md)** — VALID_SLOTS constant move + this session's hygiene logged. The T-track notes (2026-07-12) and the C7 note (2026-07-11) remain true underneath this one. **If you (next session) touch a subsystem, keep this discipline: update its ICE_Architecture.md section + any stale spec/roadmap references in the same session, then note it here.**

**Next: F10/F14** ([F10_F14_import.md](specs/F10_F14_import.md)) — conversation import (LSREP replay as migration tool) + raw-log extraction v2. **OPEN THE SESSION WITH THE USER-REQUIRED STEP: the provider exports** (ChatGPT/Claude settings → export; ~10 min) **and the decay-policy choice** (preserve / simulate-decay / start-fresh) — without the export files nothing can be built against real data. Primitives it builds on (all live as of `d4d0a79`): the store is EMPTY at 1024 — F10's replay is its first real filler; G23's D2 state-copy import is the *sibling* path (manifest `kind` distinguishes state-copy from replay — F10 re-lives history through the pipeline instead of copying rows); the re-embed runner handles any embedder drift on imported stores; store_meta's guard protects every step; T's synthetic-time provenance marking is specced for imported turns (F14). E10 stays parked until C12's backend. G22 habit stands: `uv run pytest tests/smoke -q` before every commit.

> **🔎 Standing critique: [BRUTAL_ASSESSMENT.md](BRUTAL_ASSESSMENT.md)** (2026-07-12, Fable close-out — written at the user's request). A no-deference flaw list over the whole project with a solution per flaw: the deletion-budget problem, the product-vs-paper fork, the **unbacked-up live memory DB + truncating tests (its ⚡⚡ item — CLOSED by G23, 2026-07-19: backup module + archives shipped, the one truncating test rewritten)**, import poisoning, pre-flight latency, classifier-taxonomy ROI, the pending-validation cliff, FINAL-flow volatility, and the rough-notes sweep result (F10 confirmed present; F16 + the C6 exclusion addendum recovered). **Not a work queue** — mine it deliberately; re-read once per phase.

**Implementation order (dependency-honoring):**

0. **G26** (P0 hotfix — `chat_completions` is broken at HEAD) + **G22** (smoke suite — the rails). Both in [G_mechanical.md](specs/G_mechanical.md).
1. **C7** — the world-change: in-process maintenance runtime, Celery+Redis deleted, shared-first bg model. Everything later assumes its world. [C7_scheduling.md](specs/C7_scheduling.md)
2. **Track T** (T1 → T2 → T3 → T4) — the exocortex layer; G5/G6/G7 alongside. [T_temporal.md](specs/T_temporal.md)
3. **E0 → E7** — service layer, then ICE-as-MCP (coding memory becomes usable early). [E0_E7_services_mcp.md](specs/E0_E7_services_mcp.md)
4. **D1/D2** — maintenance agent; Sentinel dies. [D1_D2_maintenance_agent.md](specs/D1_D2_maintenance_agent.md)
5. **E-coding core** — E1 → E1b → E9 → E3/E4 → E8 (E10 waits on C12's backend). [E_coding_core.md](specs/E_coding_core.md)
6. **C4/C9** then **C10/C11**. [C4_C9](specs/C4_C9_memory_quality.md) · [C10_C11](specs/C10_C11_deletion_chat_control.md)
7. **G23+C17** — export/backup, then 384→1024 (backup FIRST — user-required). [G23_C17](specs/G23_C17_data_longevity.md)
8. **F10/F14** import (+ C12 backend when reached — its entry is decision-complete as written). [F10_F14](specs/F10_F14_import.md)
9. **B1 retrain** — the user-heaviest item (see duties). [B1_classifier_retrain.md](specs/B1_classifier_retrain.md)
10. **Z1-prep** (G9 sweep → staged tuning → coverage matrix → pending-validation ledger) → **Z1, the gate**. [Z1_tuning_coverage.md](specs/Z1_tuning_coverage.md)
11. **FINAL** — the experiment redo, strictly last. [FINAL_experiments.md](specs/FINAL_experiments.md)
12. Post-FINAL: **B3** (only if its gates pass — [B3_routing.md](specs/B3_routing.md)), **F-track** (design conversation from [F_design_brief.md](specs/F_design_brief.md); F2 first), C13/C14 per the Z1 profile ([C13_C14](specs/C13_C14_caching.md)).

Mechanical G items: opportunistic per [G_mechanical.md](specs/G_mechanical.md), each inside the phase that touches its file. Architecture-doc upkeep per CLAUDE.md applies at every phase. **Boy-scout cleanup applies at every phase too** (CLAUDE.md standing rule → ledger in [docs/CLEANUP.md](CLEANUP.md)): touched files leave cleaner, one-offs → `scripts/oneoff/`, pre-FINAL experiment folders stay frozen.

**USER-REQUIRED duties (consolidated from the specs — the human's own to-do list):**

| phase | duty | size |
|---|---|---|
| 3 (E7) | register `ice-mcp` in your harness config | **DEFERRED by user (2026-07-18) — no memory worth mounting yet; do NOT remind. Revisit only when the user raises it.** |
| 5 (E-core) | register projects; approve/decline the git post-commit hook per project | minutes |
| 7 (G23/C17) | ~~run `ice_backup.sh` before the migration~~ **DONE 2026-07-19** — archive confirmed; user chose store WIPE over re-embed | — |
| 8 (F10) | export chat history from providers (ChatGPT/Claude settings → export); pick a decay policy; machine on for big imports | ~10 min + wait |
| 9 (B1) | one-time training-data consent · review labeler disagreements (~200–400 rows) · 5% audit · run + approve promotion | ~2–3 h total |
| 10 (Z1) | machine on for ≤4 sweep nights · read the tuning report's load-bearing table | ~30 min + nights |
| 11 (FINAL) | subscribe one Ollama Cloud paid month (~$20; set a cancel reminder) · laptop-on nights (~a week, resumable) · decide `--allow-personal-cloud` · optional & skippable: small Anthropic key (~$5–10) for the prestige appendix (**Pro ≠ API**) | setup ~10 min + nights |
| post (B3) | confirm registry tags (~1 min/model) · cloud toggle choice | minutes |
| standing | review-queue passes once F2 exists (agent + reflection proposals) | occasional |

**Recovery / future-self notes (when something goes wrong):**
- **Ground-truth order: current code > the item's spec > this roadmap entry > memory.** Specs declare their `Assumes` chain — read it before coding.
- **Code↔spec divergence:** STOP; re-ground in source; fix the spec and its dependents FIRST; record the divergence in the entry; then code (README rule 12). Never improvise past a mismatch — that's what the specs exist to prevent.
- **A number seems wrong** → tune it via Z1-prep's protocol (never hand-edit constants mid-implementation). **A mechanism seems wrong** → back to its spec's Decisions section; disagree there, in writing.
- **Session bootstrap:** CLAUDE.md → this section → the item's spec → its Assumes chain → the exact files it names.
- **Entry-trim status:** the heaviest superseded entries are trimmed to spec pointers; some long entries still carry full prose *plus* a spec link — **the spec is authoritative wherever they disagree**; trim further opportunistically.

---

## Phase S — Executor-handoff spec pass (time-critical meta-item)

- [x] **S1 Decision-complete specs for every still-open, design-heavy item** *(DONE 2026-07-10, one day early — 16 files in docs/specs/: 13 item-specs + F_design_brief + G_mechanical + the README rules; every priority item spec'd, every other unchecked item spec'd-or-noted; close-out delivered: top-of-file execution flow + USER-REQUIRED duties + recovery notes, sweep notes on C6/C12/B4/D3/H1/H2/H4/H5, heaviest entry trims (rest opportunistic — specs are authoritative). Saturday 2026-07-12 remains free as overflow/review.)* `(meta — user, 2026-07)` — **Context & dates:** this cycle's sessions run on Fable 5, which goes away **Sunday night 2026-07-13**; the user's plan is a **dedicated fresh session on Saturday 2026-07-12 that does ONLY this item** (no implementation work that day). *(Update 2026-07-10: limits reset — S1 started early, in the session that produced Track T; Saturday stays as overflow/continuation.)* The roadmap's entries are deliberately *intent-not-spec*, which assumes an equally strong designer at implementation time. Before the window closes, every unchecked item that involves **design judgment** (open questions, "decide when built", algorithm choice) gets a **decision-complete spec** at `docs/specs/<item>.md`, written at full reasoning depth, so a weaker executor (e.g. Opus 4.8) can implement it *mechanically* — the thinking pre-paid, only the typing left. **START HERE, Saturday session: read [docs/specs/README.md](specs/README.md)** — it holds the canonical template and the working rules (verify the model is Fable 5 FIRST; ground every spec in current code, not memory; one spec per commit; budget the day by regret — FINAL gets the largest share; S1 is done when every priority-list item has a spec or an explicit "no spec needed" note).
  - **Spec template (every spec has all seven):** ① **Decisions** — settled calls with the why (nothing left as "options: A or B" unless genuinely user-owned, and then say exactly what to ask the user); ② **Algorithm & data model** — concrete, pseudocode/SQL/schema where it matters; ③ **Files & integration points** — what to touch and what each change looks like; ④ **Edge cases & failure modes** — enumerated, with intended handling; ⑤ **Validation checklist** — the behavioral live-DB test pattern used throughout this cycle (name the specific checks); ⑥ **Look-ahead constraints** — what later items need this one to preserve; ⑦ **Traps** — what NOT to do and why it will be tempting.
  - **Rules:** an item implemented before the window closes needs no spec (its completion note serves). Roadmap entries link their spec once it exists. Specs follow the same propagate-on-completion discipline — a stale spec is worse than none.
  - **Priority order (design-heaviest / highest-regret-if-unspecced first):** **FINAL** (experiment redesign — the single most reasoning-heavy item) → **Track T (T2–T4)** (temporal exocortex, added 2026-07-10 — written FIRST, in its origin session, while the code investigation behind it was live in-context; FINAL still gets the largest budget share) → **C7** (scheduling rework + Celery-survival decision + shared-first implementation) → **D1/D2** (maintenance agent + sentinel fold-in) → **E0+E7** (service layer + MCP surface) → **E1/E1b/E8/E9/E10** (coding-ICE core) → **B1** (retrain: labeling methodology + architecture) → **C4/C9/C10/C11** → **G23+C17** (if not done in-window) → **B3** → **F10/F14** → **C13/C14** → **Z1-prep spec (parameter-tuning protocol + whole-system coverage matrix — new 2026-07-10, user)** → **F-track design brief** (pre-work for the F design conversation). Mechanical G-track fixes get one line each in a combined `docs/specs/G_mechanical.md`, not full specs.
  - **Close-out deliverables (user, 2026-07-10 — the LAST acts of S1, after every priority item has its spec):** (1) **Roadmap cleanup + master execution flow:** add a "How to execute this roadmap" section at the TOP of this file — the post-Fable implementation order (spec-linked, dependency-honoring, marking mechanical vs specced vs user-gated items); then trim each still-unchecked entry that now has a spec down to intent + key-decision one-liner + spec link (detail lives in the spec; **completed items keep their completion notes untouched** — they are the historical record). (2) **USER-REQUIRED consolidation:** collect every `**USER-REQUIRED:**` block from the specs (retrain labeling, chat-log exports, cloud keys/accounts, review passes, machine-on overnight runs) into one section of that top flow — the user's own to-do list with concrete instructions, so human-gated steps are never discovered mid-implementation. (3) **Future-self recovery note:** a short top-of-file section for the implementing sessions — what to do on code↔spec divergence (specs/README rule 12: stop, re-ground, fix the spec and its dependents first, record it, then code), where ground truth lives, and the "never improvise past a mismatch" rule. (4) **Z1-prep spec** (parameter-tuning protocol + whole-system coverage matrix) — added to the priority list, see SEMIFINAL.

## Phase 0 — Ablation review & pruning (gate for everything else)

- [x] **P0.1 Review ablation results and sentence every feature** `(rework)` — Done 2026-07 from `experiments/flaw_ablation/buildup/paper_summary.md` (67 probes, single-convo Flaw, single-pass on the mature DB, 14B judge → **directional, not definitive**; Exp2's 1,211 manually-audited probes remain the headline). The `subtraction/` run was never summarised to a `.md` (scripts only). Per-feature verdict:

  | Feature | Buildup signal | Verdict |
  |---|---|---|
  | RRF | +0.84 (largest gain; rescues BM25) | **Load-bearing — keep** |
  | keyword_boost | +0.12, recency +0.70 | **Keep** |
  | cluster_restrict | +0.01 score, **+0.66 recency** | **Keep**; value capped until clustering fixed (C5) |
  | codex | +0.03, recency −0.14 (surfaces older structural facts) | **Keep + invest** — the floor Track A raises |
  | procedural | +0.17 here (vs +0.02 Exp2) | **Keep**, widen gating (C9) |
  | BM25 (standalone) | **−0.75** alone, recovered by RRF | **Keep inside fusion, fix** the too-loose top-30 OR-query |
  | dynamic_budget | **−0.11**, added tokens (14.9k→24.5k), doubled frags | **Retune** — mistuned at high turn counts (C15/G9) |
  | sliding_window | +0.02 for big token cost | **Keep but cheapen** — revisit vs whole-convo summary (C4) |
  | session_diversify / batch_summary | ~0.00 / +0.02 (neutral single-convo) | **Keep, re-evaluate** in the multi-convo test they exist for |
  | HyDE | +0.03 here, −0.17 Exp2, already commented out | **Do NOT ship real HyDE** — see note below; supersede with grounded expansion in A4 |
  | MERA | **−0.21** | **Disable by default now; re-home into A4/A7** (P0.2) |

  **HyDE finding:** the commented `_hyde_rewrite` ([orchestrator.py:421](../src/retrieval/orchestrator.py)) is **query reformulation, not HyDE** — it rewrites the question, never fabricates a hypothetical answer document. *Actual* HyDE would likely be worse-to-wash here: it fabricates answers with the small bg model over the user's private history (hallucinated specifics corrupt the noise-sensitive BM25 leg), and adds a blocking bg call on the synchronous pre-flight path. Keep the code commented (not pruned), relabel it honestly, and pursue **grounded query expansion** (expand via NER entities + Codex neighbours, no free-form fabrication) inside A4 instead.

- [x] **P0.2 Disable MERA by default** `(rework)` — MERA is functional but structurally weak: loose trigger (fires on `what is`/`which`/`name`/`model`/`method`…), dumps up to 15 entity payloads as a flat blob, and depends on a fragile LLM tag/relation mapping that may not match the DB's actual tags. Net −0.21. Its capability (category/enumeration answering) is exactly what A4 (relation-aware primary leg) + A7 (community/topic summaries) are designed to do properly. **Disable now** (default off, keep the `mera` ablation flag) so it stops hurting; **fully remove** once A4 re-homes the capability. Build-up-not-replace: the capability survives, the weak standalone agent doesn't. *(Update: A4 done — `mera.py` deleted, capability re-homed into `_codex_enumeration`, ablation flag repointed.)*

---

## Track A — Codex overhaul (highest-leverage rework)

The experiments showed Codex is the most ambitious *and* most handicapped subsystem. These items are ordered so each builds on the last. **North star:** the end state is a *real* knowledge graph — rich per-entity payloads, community/topic summaries, relation-aware answering — not "a list of entities with arrows." A1–A6 make the base pipeline trustworthy and dense; A7–A8 make it a genuinely functional graph. Do the earlier items in a way that A7's unified/code-aware graph slots on top rather than replacing them.

- [x] **A1 Extraction chunking fix** `(rework)` — Done 2026-07. New `_chunk_text` in [codex_extractor.py](../src/workers/codex_extractor.py) does sentence-aware (prose) + code-line-aware (fenced code) chunking, greedy-packed to `CHUNK_TOKENS`=550 with `OVERLAP_WORDS`=50; code counted at heavier token density; short turns pass through as one chunk. Chunk size is shared with A2's NER pass. Mechanics validated with a standalone test (chunk sizes bounded, sentences/code lines intact, overlap present). **Pending end-quality validation** (needs GPU services + populated DB): re-extract a sample via `experiments/mature/diagnose_codex.py` / `tests/test_codex_extractor.py` and compare entity-drop + `X uses X` hallucination rate before/after.
- [x] **A2 NER-grounded extraction (hybrid CPU/GPU pipeline)** `(rework)` — Done 2026-07. Per A1 chunk, [codex_extractor.py](../src/workers/codex_extractor.py) runs the shared CPU micro-NER (`extract_entities`), injects a CONFIRMED ENTITIES block into the extraction prompt, and post-filters via `_ground_triplets`: a triplet's subject (and, for non-property relations, object) must match a confirmed entity by normalised-exact or token-subset comparison, else it's dropped. Property relations ground subject only (object is a value). Added a self-reference filter (`subject==object`) that kills `fastapi uses fastapi`. Empty-NER chunks skip grounding (no anchor). Logic validated by standalone test. **A3 seam:** `_ground_triplets` returns `(grounded, rejected)` — A3 flips the call site from drop→keep-low-confidence (one line). **NER quality was checked empirically first** (see A9): ~95% recall but with subword truncation, boundary bleed, and pronoun false-positives. Fixed the grounding-critical ones in `ner_utils` (word-boundary snapping, function-word edge-trim, junk filter, dedup) — truncation was the only flaw that broke grounding; the rest are token-subset-absorbed. **Pending live validation:** the `codex_grounding` log lines give the over-rejection rate; act on it only if it shows systematic under-coverage (answer from data, not upfront) — *and per A9's 2026-07-11 successor decision, the action is the GLiNER swap on the background tier, not a micro-NER retrain (see A9).*
- [x] **A3 Confidence-calibrated edges + confidence/strength as a live retrieval signal** `(rework)` — Done 2026-07 (completed properly after a first pass that shipped only half and was wrongly checked off — the full scope combines the *original* entry, confidence-calibrated storage with dynamic retrieval thresholds, with the *new* points from discussion: retrieval reinforcement + smarter traversal). **Corroboration-trap correction stands:** "pending edges are never retrieved" is false in code (second paper-vs-code myth after NER — see correction block); retrieval always filtered only on `valid_until`. Shipped:
  1. **`codex_edges.extraction_confidence` Float ∈ [0,1]** (models + alembic `b7e2a91c4f05`, applied) — extraction trust, orthogonal to `strength` (usage dynamics). **Effective trust = strength × extraction_confidence.**
  2. **A2 seam flipped** ([codex_extractor.py](../src/workers/codex_extractor.py)): grounding-rejected triplets are **stored at CONF_REJECTED=0.35** instead of dropped (grounded 0.9, no-NER chunk 0.7); dedup keeps max confidence; `handle_triplet` seeds new edges and raises to max on corroborating re-extraction.
  3. **Dynamic thresholds at retrieval** ([orchestrator.py](../src/retrieval/orchestrator.py)): direct edges expand/anchor only at trust ≥ 0.5 (rejected-seed edges stay out of context until corroborated); deep hops require trust ≥ 1.0 (kills depth-3 pollution); fragment score graded 1.0–1.5 from mean trust; context_payload lists edges strongest-first.
  4. **Closed reinforcement loop:** `_reinforce_codex_edges` (+0.15 on anchors, cap 10.0, write-on-read like episodic) now **promotes** pending→active at strength ≥ 2.0 *iff* confidence ≥ 0.5; `codex_decay` decays **all** live edges (was active-only — a reinforced pending edge previously inflated forever) and **expires** never-corroborated pending edges below 0.1 (garbage-collects hallucination residue).
  **Validated 15/15 against the live DB** (scratch functional test: seeding, corroboration-max, both traversal gates, anchor selection, reinforcement, confidence-gated promotion, cleanup verified). Remaining live-usage check: reinforcement/decay balance over real conversations. Per-edge granularity in fusion still awaits A10.
- [x] **A4 Relation-aware retrieval** `(rework)` — Done 2026-07, validated 12/12 against the live DB. Shipped in [orchestrator.py](../src/retrieval/orchestrator.py): (1) **`_detect_relations`** — two LLM-free channels: lexical (relation content-words in the prompt after two-sided stemming; "who inspired X" → `inspired_by`) + embedding (true-cosine prompt↔gloss, top-5 over 0.45 floor, for paraphrases; embeddings re-normalised because truncate_dim=384 breaks unit norm). Empirical rule: detection is *joint-signal only* (neutral prompts score ~0.69 absolute vs random glosses) — precision lives in the entity∩relation join. (2) **Relation-fact surfacing** — matched-entity edges in detected relations (either direction, trust-gated) rendered as `[Fact: a --rel--> b]` lines, added to reinforcement anchors, `+0.25` overlap boost (user's boost idea; ceiling 1.75). (3) **Payload descriptor fallback** in entity resolution — "main fortress" → ILIKE over context_payload → Obsidian Citadel (payload *embeddings* still future work). (4) **Enumeration re-homed from MERA** (`_codex_enumeration`): enum-cue AND grounded signal (prompt-token→tag match, or detected relation) → tagged entities' payloads + trust-gated fact lines; no LLM; `mera.py` **deleted** (ablation `mera` flag now toggles this equivalent capability; legacy test guarded). (5) **Grounded query expansion** (HyDE replacement): codex leg runs first; matched canonical names + aliases (≤8) appended to the BM25 search prompt; nothing generated. *Deferred to A9: the "NER/relations retrain" half — revisit if live relation-detection logs show lexical+embedding channels under-covering.*
- [x] **A5 Codex conversation-scoping division (retrieval side)** `(bug)` — Done 2026-07 (validated in the A4 test run: in-scope edge present, out-of-scope edge excluded, global payload not leaked). Under a project-scoped conversation, `_codex_scope_sets` resolves the batch + entity sets once and traversal honors them throughout: edges filtered by `source_batch`, expansion never leaves the conversation's entity set, and per-entity payloads are **rendered on the fly from the conversation's own edges** instead of the stored global context_payload — a shared entity ("ice") no longer leaks other conversations' facts. Auto/none conversations keep global traversal (per the scoping semantics in main.py). *Extraction-side division (per-project graphs / entity namespacing) deliberately deferred to A7's unified-graph design — retrieval isolation was the bleeding wound; storage partitioning is an A7 schema decision.* **Look-ahead vs C6 (scoping rework):** A5's filtering primitive is a *set of batch_ids* — and C6's cross-chat (N conversations), session_id, and @-mention scoping all resolve to exactly that set, so the traversal filters never need to change. `_codex_scope_sets` was generalised now to accept `scope["batch_ids"]` / `["conversation_ids"]` / `["conversation_id"]` / `["isolated"]` (empty-set = true incognito, the C6/G16 'none' semantics) so C6 only has to populate the scope in main.py — no orchestrator rework. **No conflict; A5 is forward-compatible.** (main.py still only sets `conversation_id` for project scope — populating the richer forms is C6's job.)
- [x] **A6 Self-correcting graph (reconciliation loop) — bounded** `(new)` — Done 2026-07, **deterministic-first per user direction (less LLM)**; validated 13/13 against the live DB (LLM decision paths via stub — real-model call `make_llm_reconciler` pends the bg model, like A1/A2 extraction). In [codex_extractor.py](../src/workers/codex_extractor.py): `check_conflict` is a cheap deterministic pre-filter (DB query only when the relation has a known **antonym**, or a **multi-valued** relation meets a **supersession cue** in the turn — else dict-lookup fast path). `reconcile_conflict` resolves it: **antonym reversals deterministic** (newer state expires its opposite for the pair, no LLM); **ambiguous supersessions** are the *only* LLM case — a bounded one-word reconciler returns `expire_old`/`keep_both`/`reject_new`, and anything else (or no reconciler) → `review_queue` row with the new edge kept (never auto-expire on a guess). `handle_triplet(turn_text, reconciler)` + standalone `check_conflict`/`reconcile_conflict`/`make_llm_reconciler` are callable units so **Track D's agent can drive the loop with its own reconciler** (look-ahead satisfied). Recency: reconciliation treats the new assertion as newest (deterministic newer-supersedes) — retrieval-scoring recency is separately **A11**. **Caveats carried:** review-queue fallback isn't user-visible until F2; the LLM half needs live-model validation before it's proven; entity-merge conflicts deliberately out of scope (needs candidate-duplicate detection — its own item). Pairs forward with Track D (agent wraps this) and A8 (asserted-negatives will make more supersessions detectable at extraction).
- [x] **A7 Codex V3 — a typed, bidirectional, rich-note codex ("Obsidian for LLMs, but better")** `(new)` — **Vision (user):** codex as an Obsidian-style knowledge graph — not just relationships between entities, but each node a *rich note* with real info about itself, navigable node→node in both directions. **Decision (delegated to Claude): defer the heavyweight Louvain+LLM-summary Graph-RAG community layer** → it can't be validated to improve anything on today's sparse conversational graph (would be complexity for its own sake); build it when the graph is denser and/or with the code graph (E1b) where dense import/call structure makes communities meaningful. Instead build the *richness* that serves the vision and is validatable now, in slices:
  - [x] **A7.1 Typed + rich bidirectional notes (storage layer)** — Done 2026-07, validated 10/10. Added `codex_entities.entity_type` + `description` (migration `c9f4d21a8b30`, applied). `_regenerate_context_payload` now builds an Obsidian-style note: description (note body) + Properties + **Links** (outgoing) + **Backlinks** (incoming — previously invisible; half of every entity's connections). `_infer_entity_type` types nodes from their relations (domain-general vocab — coding/research/academic/business/personal/creative all covered, synonym-folded; tag wins over votes). `description` is filled by A7.3. **Domain-general by design** (per user): the rich-note mechanism summarises whatever a turn said about an entity — a function, a research finding, a business metric, a person, a character — the type vocab is just convenience with inference for common cases; entity_type is open to any tag.
  - [x] **A7.2 Retrieval navigation** — Done 2026-07, validated 6/6. `_traverse_graph` now follows **both** directions (outgoing links + incoming backlinks, trust/scope-gated) so navigation works both ways; `_render_codex_entity` does **depth-graded rendering** (anchor = full note; neighbors = compact `[name (type)]: snippet` preview — the Obsidian reading model, token-efficient). This also resolves the old "depth-3 BFS is dumb" complaint: deep entities are now cheap previews, not full payloads. Each matched anchor keeps its own fragment (per-anchor visited + other-anchors excluded from absorption), preserving A10. No regressions (A3 15/15, A4/A5 12/12, A6 13/13, A10 9/9).
  - [x] **A7.3 Enrichment engine (rich note bodies)** — Done 2026-07, validated 5/5 (LLM stubbed, real bg-model call pends the model — like A1/A2/A6). Upgraded reflection `_enrich_codex_entities`: now writes the LLM summary to **`description`** (the note body) — not `context_payload`, which is auto-assembled and would be overwritten — then calls `_regenerate_context_payload` so the note = description + properties + links + backlinks. Selection prioritises **empty-description entities with the most mentions** (25/run, up from 10), then refreshes stale well-mentioned notes (>14d). Domain-general prompt (person/software/function/concept/org/event/product/lore). **Backlog is real: the dev graph has ~1,150 entities awaiting a note body** — this worker fills them once the bg model runs. This is the slice that turns nodes from labels into real notes.
  - [ ] **A7.4 Community layer (deferred)** — Louvain/Leiden community detection + hierarchy-ready LLM summaries for whole-forest overview queries. Build when graph density justifies it. *(Update 2026-07-18: the code graph it was waiting for EXISTS — E1b shipped `source='static_analysis'` entities/edges under per-project namespacing (`fe8df63`); dense import/call structure is exactly where communities pay. When built, partition per project + source, and remember derived entities are journal-free — community summaries over them must not write CodexEvents either.)*

  *Original (superseded) framing follows:* **Scope settled 2026-07 (do A10 first — it benefits A7's retrieval):** build the Graph-RAG *community* layer on the existing conversational codex — community detection over the entity/edge graph + an LLM summary per community, so broad/overview queries ("how does the political system work", "summarize the tech stack") hit a community summary instead of traversing 30 entities — plus an `entity_type` column so entities are typed (character/location/concept/…), which the code graph later reuses. Aim for the robust version (hierarchy-ready communities, proper detection algo, not flat toy clustering). The **code-aware half** (AST entities, deterministic `imports`/`calls`/`inherits` edges from a real codebase) is **moved to Track E** — it can't be meaningfully built without Coding Mode's ingestion/pipeline; A7 only settles the *one-graph* schema so code entities slot into the same `codex_entities`/`codex_edges` tables. Original (superseded) framing follows: Add a deterministic static-analysis layer: AST-level entities (deterministic IDs from definition sites), deterministic edges (`imports`, `calls`, `inherits`, `defined_in`, `tested_by`), and Graph-RAG-style community summarisation (Louvain/Leiden over the import/call graph, LLM summary per community). **Constraint from the notes: there is ONE Codex** serving both conversation and coding modes — not two graphs. OKF is adapted here as design philosophy (typed knowledge units in our tables, not markdown files). Like a real proper graph rag inside our entity type codex that we have. PLUS the coding based if it is ever given a code base then it should be able to make like relations between files, functions and all. do if if not done before.
- [x] **A8 Codex relation negation / polarity** `(new)` — Done 2026-07, validated 8/8. Added `codex_edges.negated` (migration `d4a1e8c37b90`) so the graph stores the *negative* of any relation without doubling the vocabulary (X distrusts Y = relation `trusts` negated). Extractor prompt detects negation → `"negated": true`; `handle_triplet` **retracts** the matching positive edge and stores the negative as its own fact; `_regenerate_context_payload` renders a distinct `Negations:` section; retrieval renders negated edges but never *navigates* them (a negation isn't a link). Complements A6 (implicit "migrated off" retraction) with explicit negatives. *Chose a polarity flag over a bigger vocabulary — solves the token-budget tension noted below.* Original note: The current set arent that diverse, for example there is not negetive version of exisiting relations so the ai chooses the positive instead, also we cant just make it wayy bigger as that also counts into what is being sent to the model. *(Confirmed from the vocab: `friend/enemy` and `married_to/is_divorced_from` have negatives, but most relations are positive-only — `uses` has no "stopped_using/deprecated", `trusts` no "distrusts", `works_with` no "conflicts_with", and there are no temporal "former_X" forms. When a fact is negative/absent the extractor skips it or grabs the positive → a false edge. valid_until expiry partly covers "no longer true" but not asserted-negative facts. The token-budget tension is real: the whole vocab is rendered into every extraction prompt. This is the relation-side analog of A2's entity grounding — likely wants a small set of high-value negatives + a "negated"/polarity flag on edges rather than doubling the vocabulary.)*
- [ ] **A9 Context-aware NER rework** `(new/open)` — The micro-NER is context-free: each subword token is embedded in isolation and classified by an MLP, so it can't use surrounding words. Empirically ~95% recall but with verb-led boundary bleed (`uses PostgreSQL`) and descriptor false-positives (`fire mage`, `very sad`, pronouns). The cheap post-processing fixes shipped with A2 (word-boundary snapping, function-word edge-trim, junk filter) handle the grounding-critical cases, but the root fix is a context-aware tagger — embed tokens **in context** (per-token hidden states over the whole sentence) or a proper token-classification head, then retrain. Do this only if A2's live over-rejection logs or retrieval quality show NER is the bottleneck; recall is already good, so this is precision/boundary polish, not urgent. **GATED — deliberately NOT built:** it's an offline model retrain (new architecture), not a code change, and there's no evidence yet it's the bottleneck. Revisit after the Z1 full system test produces the over-rejection data. Building it now would be speculative complexity. *(If/when it happens: train at 1024-dim inputs per C17 — until then NER slices the 384-prefix of the native embedding — LIVE since C17 (2026-07-19): ner_utils consumes `src/memory/embedder.slice384()`.)* **Successor decided (2026-07-11, user + external-critique review): when the gate trips, the answer is a GLiNER-class off-the-shelf zero-shot NER model for the background side, NOT a micro-NER retrain.** Rationale: unlike the classifier (bespoke label taxonomy ⇒ must train our own head), NER's label space is genuinely generic, and GLiNER's inference-time label conditioning covers ICE's outlier types (fictional characters, project names, functions, datasets) as a **config list of label names instead of training data** — it's a full pretrained context-aware model (~200–500M, CPU-viable, Apache-licensed variants), not an embed-plus-own-head build like the classifier. **Split decided (user, 2026-07-11): two-tier NER by path.** (a) **Background/post-flight extraction** (codex extraction chunks in `codex_extractor`, clustering's `_extract_turn_entities`, C4's summary must-terms — the accuracy-sensitive, latency-insensitive side) switches to GLiNER behind the existing `extract_entities()` seam — A2 grounding (token-subset matching) and A3 confidence seeding untouched, only the entity source improves. (b) **Pre-flight** (the synchronous A4 grounded-query-expansion call on the user's prompt — latency-sensitive) **keeps the micro-NER** (near-zero cost vs GLiNER's ~50–150ms CPU). The seam therefore grows one flag: `extract_entities(..., tier="background"|"preflight")` or two thin wrappers — either way one module, two backends. (c) **Micro-NER retrain stays an optional later upgrade** for the pre-flight tier only — strictly positive (background quality no longer depends on it), same empirical gate. *(Supersedes the C17 parenthetical above for the background half: GLiNER doesn't consume the shared embedding, so background NER exits the 384/1024 migration story; the pre-flight micro-NER keeps the slice384 staging until/unless its optional retrain.)*
- [x] **A10 Codex leg representation + budget fairness** `(new)` — Done 2026-07 (sequenced before A7 as its prerequisite), validated 9/9 against the live DB. `_codex_graph` now emits **one fragment per anchor entity** (payload + trust-gated neighborhood + that anchor's relation facts), each scored by its own direct-edge trust — no more single blob capped at one budget slot; enumeration likewise emits per-entity fragments. `_enforce_token_budget` phase 2 replaced with **round-robin-with-slack** across legs (balanced when all legs have fragments; fills fully when sparse — verified episodic no longer soaks the remainder, and codex/procedural get ≥2 slots when they have them). A7's community summaries will slot in as additional codex fragments. *Original entry:* The codex leg collapses its entire traversal into **one** concatenated `ContextFragment` ([orchestrator.py](../src/retrieval/orchestrator.py) `_codex_graph`), so in `_enforce_token_budget` it can occupy at most one slot no matter how relevant, while episodic emits dozens of fragments and soaks the greedy fill — a structural reason codex was only 3.3% of fragments (not just extraction weakness). Fix: (a) return **multiple scored fragments** (per entity/subgraph) so each competes in fusion/budget on its own relevance and irrelevant depth-3 entities drop out instead of bloating one blob; (b) **budget-fill fairness** — per-leg floor/quota in `_enforce_token_budget` so episodic stops dominating the remainder (affects procedural too). This is the companion to A4 (relation-aware retrieval naturally yields per-relation fragments) and where A3's per-edge strength becomes per-fragment score. Confirmed via code audit 2026-07; raised by user (episodic-preference intuition — correct). **Sequenced before A7** (community summaries need to surface as first-class fragments, not be jammed into the blob). Robust design: one fragment per *anchor entity* (its payload + trust-gated neighborhood + its relation facts), scored by that entity's effective trust; budget fill uses **round-robin-with-slack** across legs (balanced when all legs have fragments, fills fully when some are sparse) so episodic stops soaking the remainder without starving it either.
- [x] **A11 Recency-weighted edge scoring** `(new)` — Done 2026-07, validated 6/6. `_edge_trust` now = `strength × extraction_confidence × recency`, where recency = `1 + CODEX_RECENCY_BOOST·exp(-age_days/τ)` (0.3, τ=30d) using `valid_from`: a recently-asserted fact outranks a stale one of equal strength, and — deliberately — old edges tend to ×1.0 (no penalty, since decay already down-weights them via strength; no double-penalty). Made `_edge_trust` an instance method to read the constants. Original note: Codex edges carry `valid_from` (assertion time) but retrieval never uses it — effective trust is `strength × extraction_confidence` only. Add a recency factor so a recently-asserted/updated fact outranks a stale one of equal strength — the codex analog of episodic's recency boost (§6.5). Small, A3-family (extends `_edge_trust`), and distinct from A6's *write-time* newer-supersedes decision (which uses `valid_from` to reconcile, not to score). Raised by user (timestamp as new/old denotor). Fold the decay-vs-recency interaction in carefully: decay already down-weights unreinforced edges over time, so recency weighting should reward *recent assertion*, not merely penalise age twice.

## Track B — Classifier & routing

- [ ] **B1 Classifier retrain: multi-label context-reliance + cloud toggle** `(rework — DEFERRED: retrain + data + cross-track consumers)` — Real_Time_Search is orthogonal (a Zero_Shot or LTM prompt can *also* need real-time), so context-reliance shouldn't be single-label 3-way. Add a toggleable "cloud" routing signal for users with API models. The Coding side may need new intent labels too.
  - **Why deferred (2026-07):** it's an offline model retrain (ctx head softmax-3 → independent sigmoids so Real_Time_Search is orthogonal), it needs a **relabeled** dataset, and its new signals have **no consumer yet** — Real_Time_Search routes nowhere until **F7** (web search), the cloud toggle needs **F11** (cloud models) + **B3** (routing policy), and the coding intent labels are gated with **E2**. Recommendation: retrain **once**, bundled with E2's coding labels **and T2's temporal-recall label (Track T)**, when F7/F11 exist — not twice.
  - Labeling methodology, head architecture, the context-prefix train/inference mismatch, and the full code-ripple audit are ALL settled in the spec. **→ spec: [docs/specs/B1_classifier_retrain.md](specs/B1_classifier_retrain.md) (S1 2026-07-10 — schema v2: 4-sigmoid ctx head {Needs_Memory, Temporal_Recall, Needs_Live_Info, High_Complexity}, +2 coding intents; trunk+3-heads at 1024-dim; two-labeler methodology, context-prefixed rows, hard-negative pairs; DI3 deletion rule; USER-REQUIRED review/audit/consent blocks.)** *(C17 shipped 2026-07-19: native-1024 encode is live everywhere; B1's retrain retires the classifier-side `slice384` staging — including `fine_tune.py`/`dataset.py`'s slicing.)*
- [x] **B2 Replace the hard LTM override with a principled, classifier-trusting combination** `(rework)` — **Done 2026-07, validated 23/23** (`tests/test_memory_decision.py`, pure-logic run: confident Zero_Shot survives a short *and* a bumped-creative turn; neutral/uncertain prefers memory; a low-`p_ltm` turn flips only once out-of-window history accumulates; each bump behaves; classifier confidence finalization for both ML and DI3 paths). Shipped `src/api/memory_decision.py::decide_memory_retrieval` — the single posterior below — and **removed all four** old forces: `_apply_hard_overrides` (gone from `classifier.py`), DI3's reference force (now a `reference_signal` bump), main.py's `turn_count>10 / conf<0.95` block, and the orchestrator's `Zero_Shot+conv / Creative ⇒ LTM` re-force (left only defensive early-returns). `ClassificationResult` gained `p_ltm / p_rts / ctx_confidence / reference_signal`; all weights are settings (`ltm_*`); the `memory_decision` breakdown is logged (F5 candidate). Architecture §2.4 rewritten. **Connecting-piece fix:** because B2 genuinely skips retrieval on confident standalone turns (the old design forced it nearly always, incidentally always injecting slots), memory-slots + bookmark injection + prompt assembly were **hoisted out of the retrieval gate** in main.py so persistent user memory injects on *every* turn — only retrieval `fragments` are conditional. **Design settled in-session (2026-07):** decide retrieve/no-retrieve from a posterior in **log-odds space** that *prefers* LTM but never *forces* it: `logit(P_need_mem) = logit(P_clf(LTM)) + β·logit(P_len) + Σ feature-bumps + bias`, retrieve iff `> τ`.
  - **Real confidence first:** today `max_confidence = max(all 25 probs)` measures topic/intent peakedness, NOT context-reliance certainty, yet it gates LTM. B2 reads `P(LTM)` directly from the ctx-softmax mass + a `ctx_confidence` margin (both added to `ClassificationResult`).
  - **Memory-pressure length prior (not a turn cliff):** `P_len` is a smooth function of how much conversation sits **beyond the sliding window** (≈ `turns_beyond_window` and/or accumulated tokens / total context so far), *not* raw `turn_count>10`. Rationale (user): if the sliding window already carries the recent turns, a recent reference is already covered; LTM earns its keep for history the window can't see.
  - **Old hard signals → smart bumps:** Creative-topic and referential/anaphora become additive log-odds bumps (tunable/learnable later), not slams — coding self-reference and creative callbacks both plausibly raise memory-need, but neither should force it (user unsure creative should always force — correct, so bump not slam).
  - **All weights in settings (G9-aligned) because B2 is built on a classifier B1 will retrain:** τ, β, ramp midpoint/steepness, and bump sizes live in settings. **We cannot know whether the B1 retrain lands better or worse**, so B2 consumes `P(LTM)` as a *scalar* (survives B1's softmax→multi-label-sigmoid swap) and is built to be **re-tuned, not rewritten**. This dependency is explicit in code + here.
  - **DI3 (user: "make it better or decide if we want it"):** DI3 *is* used (seen in experiments) — keep its cheap topic/intent fast-paths, but demote its context-reliance from a hard force to a prior into the same combination. The full "do we keep DI3 at all" call is deferred to **after B1** — **with a firmed default (2026-07 audit): plan for deletion, keep at most the noise gate.** Rationale: hand-tuned lexicon-density classification has a hard ceiling that no amount of threshold tuning lifts; its one unique contribution (fast anaphora) is already just a bump into B2's combination; and a context-aware retrained classifier covers its remaining cases. Only keep more if B1's retrained model measurably loses to a DI3 fast-path somewhere.
  - **Seam for B3:** the clean `ctx_confidence` posterior is exactly what the learned router wants.
- [ ] **B3 Learned MoE routing** `(rework — DEFERRED: needs training signal + real specialist models)` — Current router is a hardcoded overlap scorer (`find_best_model`: topic+intent overlap + priority); empirically neutral-to-harmful (≈−0.02 Exp2, −0.04 Exp1) and blind to confidence and context-reliance. Wanted: confidence-weighted routing, Zero_Shot→small-model / LTM→big-model policies, a lightweight bandit over empirical per-topic performance, and model-load integration (preload/keep-warm to kill the 5–15 s Ollama swap spike). LSREP replays can train the policy offline.
  - **Why deferred (2026-07):** (1) needs a **training signal** — per-(topic,model) empirical reward — which only comes from the **experiment redo (FINAL)** + LSREP replays; (2) needs **specialist models actually worth routing to** (FINAL note pt.2: MoE was neutral because the pool isn't specialised enough — a model-curation problem, not code); (3) keep-warm/preload ties to **G8** (Redis sticky state) + **G4** (GPU gating). **Seam held now:** keep the `find_best_model(topic, intent, required_tokens)` signature stable so the learned policy slots in behind it; B2's `ctx_confidence` posterior is designed to feed it. **Registry tag quality is part of this fix (2026-07 audit):** the router can never beat its tags, and today's auto-tagging is garbage-prone — `_ollama_name_to_hf_id` name-guesses HF repos (several mappings don't exist), and the HF-tag→ICE-label maps yield 1–2 coarse tags. Delete the HF guessing during the rework; tags should come from the bg-model tagger + user confirmation (F4 registry editing). Also ties to C16 (model-class-first selection reorders budget↔routing). **→ spec: [docs/specs/B3_routing.md](specs/B3_routing.md) (three gates; two-stage policy — deterministic class selection + ε-greedy reward table seeded from FINAL, F9-only online updates; registry tag cleanup + keep_warm folded).**
- [ ] **B4 Feedback loop + fine-tune promotion (close the broken loop)** `(bug/planned)` — Thumbs-up/down per response: up ⇒ auto-add to curated set; at ~100 downs, offer to auto-label with a strong model, then user reviews via toggle-button UI; feature OFF by default for inexperienced users. Critically, fix promotion: today `fine_tune.py` loads hardcoded `v2_final.pt`, writes `finetuned_{ts}.pt`, and the live path is `v3_qwen_ft3.pt` — the weekly fine-tune changes nothing. Auto-promote the new checkpoint to `settings.classifier_model_path` (with restart/reload). **Split (2026-07): the *promotion* half is DONE (pending live validation) — `fine_tune.py` now trains from the live checkpoint, holds out 20%, and only promotes (backup `_prev_{ts}.pt` + atomic replace of `settings.classifier_model_path`) when the candidate's held-out loss ≤ the live model's and there are ≥20 curated rows; G1 path-unification landed with it (`drop_zone.py` too). Logic is written + compiles, but a real promotion run needs GPU + a populated `curated_labels` table → validate in Z1. Two pieces stay deferred: hot-reloading the running proxy without a restart, and the *feedback-collection* half (thumbs UI → `CuratedLabel`) which is frontend → [F9](#track-f--frontend--ux-custom-frontend-era). This item stays unchecked until those + live validation.** **S1 note (2026-07-10): no spec needed** — promotion half pends only Z1's live run; the feedback half is F9's UI (B1's spec pins CuratedLabel schema-v2 compatibility).
- [ ] **B5 Ensemble classifiers** `(open — parked)` — Soft-voting over 2–3 heterogeneous classifiers. Explicitly an *extreme-scenario-only* option if retraining can't hit accuracy targets. **Gated on B1:** only revisit if B1's retrain (with the labeling/architecture rework above) still can't hit accuracy targets.
- [ ] **B6 Tree conversation** `(open — DEFERRED to Track F)` — The ability to change the previous msg or any message and effectively creating a branch from that message as if a new convo and handling the memory for that, the pre-requesits are there in the tables form what i remember, and is alaso tied to the frontend. **Why deferred:** frontend-bound — needs **F13** (session replay + branching; `session_replays` is written by nothing today) plus branch-lineage scoping in retrieval (a branch = new conversation lineage forked at a turn) and memory-inheritance rules (branch inherits ancestor memory up to the fork point). Build with the F13 frontend. **Branch deletion is already solved (C10, 2026-07-19):** `services/conversations.py::delete_conversation` is the per-conversation cascade — B6 deletes a branch by running it over the branch's conversation set (lineage = conversation set, A5's batch-set primitive again); don't build a second deletion path.

## Track C — Retrieval & memory quality

> **Sub-project map (2026-07 planning — code-grounded, nothing started yet).** The 15 items cluster into 5 sub-projects; work them by sub-project, not by number:
> - **① Representation & density** — `C1` raw-vs-summary decider + summary quality, `C2` big-input chunking, `C3` chunk-aware retrieval + hierarchical summaries, `C17` embedding un-truncation (384→1024, bundled with the re-embed the chunk rework forces anyway), `C12` doc pipeline. **Share one primitive** (chunk + abstract→medium→full summary + relevant-chunk retrieval); build it once. Mostly backend-doable now — only C12's *upload UI* is Track-F-gated (the drop-zone already ingests files, so the pipeline isn't blocked).
> - **② Scoping & session structure** — `C6` session_id + scope semantics (codex side already done via A5), `C5` clustering fix, `C10` deletion cascade, `G16` none-incognito. C6's *backend* (session_id, episodic-leg scoping, incognito) is doable now; its *sidebar toggles / @-mention* are Track-F.
> - **③ Background maintenance** — `C7` session-start decay/worker catch-up (**absorbed later by the Track-D agent → build composable, not a patch**), `C8` time-weighted vector (small), plus adjacent ops bugs `G10`/`G11` and the unscheduled `cluster_merge` worker.
> - **④ Procedural & user control** — `C9` procedural + slots widening, `C11` chat memory commands (command-parsing backend now; command UI is Track-F).
> - **⑤ Performance/caching** — `C13` caching, `C14` KV-cache. **Premature, not blocked** (see C13) → defer.
>
> **Recommended sequence (dependency-driven):** ② first (**C6 session_id primitive → C5 clustering fix** — session_id is a small time-gap-heuristic column that makes clustering session-aware so C5 is fixed *once*, and it's also C7's "gap since last session" hook), then ① the density sub-project (C1→C2→C3, C12 backend), then ③ (C7 + fold in the quick wins C8/C15), then ④, defer ⑤. **Pattern (like A5 did for C6's codex side):** build the backend primitive now, leave a clean seam for the Track-F UI surface — don't wait on the frontend for the backend.
>
> **Two bugs found while reading (fix inside the relevant rework, not as standalone patches):**
> 1. **`post_flight.py` inject_raw clobber** ([post_flight.py:145-150](../src/workers/post_flight.py)) — the document branch sets `turn.inject_raw = True`, but the next line overwrites it with the earlier local `inject_raw` (usually `False` for a summarized long turn), so documents **don't actually get raw injection** — the opposite of intent. Fold into C1/C2.
> 2. **Unscheduled/broken workers** — *corrected by the 2026-07 full audit:* the celery `include` list referenced a **nonexistent** `src.workers.cluster_merge` module, which crashed worker boot entirely (fixed, G21). *(Historical: celery_app.py itself was deleted 2026-07-11 by C7 — scheduling now lives in [runtime.py](../src/workers/runtime.py)'s JOBS table + `settings.maintenance_intervals`; cluster merge runs every 3h, compaction every 24h — G10 fully settled.)*
>
> **Structural note — orchestrator decomposition:** [orchestrator.py](../src/retrieval/orchestrator.py) is a 1,550-line God-class (six legs + fusion + bonuses + budget + codex traversal + enumeration + relation detection + wide-net). Don't do a standalone refactor; **decompose it incrementally as ①/② items open it** (legs / fusion / codex-traversal modules), so each C-item leaves the file smaller than it found it.

- [x] **C1 Lossless / inject_raw rework + better summarisation** `(rework)` — **Done 2026-07, validated 32/32** (`tests/test_turn_density.py`: density scoring, storage matrix, grounded-summary retry, read-time chooser all 8 branches, degrade-before-drop, live-DB `evaluate_turn` integration incl. the clobber fix; real bg-model summary *quality* pends Z1 like all LLM halves). **Design settled in-session — the user's key decision: STORE BOTH, CHOOSE AT READ TIME.** Raw and grounded summary are both stored for every non-document turn >350 words; nothing is ever permanently one representation; `inject_raw` demoted to a default hint. Shipped: (1) [turn_density.py](../src/workers/turn_density.py) — `entropy_score` **finally computed** (facts-per-token: NER entity density + figures/identifiers + code + lexical diversity; resolves the G20 implement-or-drop question) and the storage matrix (document/short → raw-only; long code/creative → raw hint + summary stored; long dense/diffuse → coverage gate sets the hint). (2) **Grounded, measured summaries**: must-preserve terms (NER + figures + identifiers) injected into the prompt (A2's ground-then-generate), 4–6 sentences + `Key terms:` line, **coverage measured + one retry naming dropped terms**, stored in new `episodic_memory.summary_coverage` (migration `f2a91b3c8d44`) — "a summary that drops the fact you'll ask for is worse than raw" became a *gate*, not a hope. (3) **Read-time chooser** (`_choose_representation`): trust (coverage ≥0.7) → keyword protection (matched term only in raw ⇒ raw, non-degradable) → intent preference (Factual/Troubleshooting → raw; Analysis/Planning/Ideation/Exploration → trusted summary) → hint. (4) **Degrade-before-drop** in `_enforce_token_budget` (both phases): a too-big raw fragment swaps to its trusted summary instead of being lost — "more with less" exactly where budget bites. (5) **Clobber bug fixed** (documents keep raw injection). (6) Old always-fired `is_lossless` replaced; `lossless_flag` (codex-extraction gate) = code ∨ creative ∨ entropy ≥0.35, kept generous. Fixed en route: `_NUM_RE` trailing-`\b` bug that stripped units off figures ("42%"→"42"). New telemetry: `summary_quality`, `representation_decided` (F5). **Look-ahead:** C3's hierarchy = more levels for the same read-time chooser; C2 owns chopping documents (matrix defers them intact by design).
- [x] **C2 Big-input handling** `(rework)` — **Done 2026-07, validated 15/15 live-DB** (`tests/test_document_chunking.py`: shared-chunker aliases, worker idempotency, catch-up healing, chunk-level vector hits on a buried fact, parent-join privacy both directions, BM25 chunk injection, legacy fallback). Shipped: (1) **the shared chunker** — A1's sentence/code-aware greedy packer extracted verbatim to [src/memory/chunking.py](../src/memory/chunking.py); codex_extractor consumes it via aliases (one implementation for extraction windows, document chunks, and future C3/C12). (2) **`episodic_chunks`** (migration `a3d47e91c256`, CASCADE from the parent turn — C10 look-ahead) + [document_chunker.py](../src/workers/document_chunker.py): event-driven from post-flight per new doc + a beat catch-up (2h) that heals pre-C2 legacy docs; private docs chunked too (visibility through the parent join). (3) **Retrieval**: the vector leg *excludes* `is_document` rows (whole-doc embeddings are mush — the C3 ceiling) and searches **chunk embeddings** instead (≤3/doc, provenance → parent turn so strengthening/decay land right); BM25-found doc rows inject their **keyword-relevant chunks** (≤2, opening-chunk fallback); the `word_cap=999999` whole-doc dump is dead — legacy unchunked docs cap at 500 words until healed. Composable callables (`run_chunk_turn` / `run_pending_documents`) per the C5/C7 pattern. *Note: this deliberately replaces the ICE-Dev-era "inject docs in full" win mechanism with relevance — re-measure at Z1/FINAL.*
- [x] **C3 Extreme-density retrieval (ICE-Dev follow-up)** `(new)` — **Done 2026-07, validated 16/16 live-DB** (`tests/test_density_c3.py`; +15/15 C2 and 32/32 C1 suites still green — 107 checks across the density stack). All four scope points shipped: (1) **chunk-aware retrieval for ALL long turns** (>600 words, not just documents — post-flight dispatch + catch-up SQL length proxy; non-doc parents stay in turn-level vector search, their chunks compete alongside with **parent-dedupe**, doc parents stay excluded); (2) **hierarchy complete: raw → summary → abstract** — one-line `abstract_text` generated in the *same* LLM call as C1's grounded summary (zero extra inference, migration `b9e4f7a2c810`), attached to fragments under the same trust/keyword-protection rules, consumed only by the budget's degrade chain (never *preferred*); (3) **section-level decomposition** — markdown headings are hard chunk boundaries in the shared chunker (no overlap across sections); (4) **smarter truncation** — word caps cut at sentence boundaries. **Honest deferrals:** chunk embeddings are 384 until C17's re-embed (runner covers `episodic_chunks`; the "rebuild once" ideal was consciously relaxed — re-embedding historical chunks is minutes of CPU); per-chunk summaries / BM25 chunk index not built (BM25 finds parents via raw text + injects keyword-relevant chunks — no validated need for more yet). *Original entry:* Chunk-aware retrieval (retrieve chunks, not turns), hierarchical summaries (abstract → medium → full), section-level document decomposition, smarter truncation. Why: the vector baseline collapsed to 94.2% failure on ICE-Dev because giant turns blew the context window; ICE won there (4.33 vs 1.23) but the same failure class threatens ICE at higher density. **Ceiling being removed (2026-07 audit):** today one 384-dim vector represents a possibly-4,000-word turn — semantic mush no tuning can fix; chunk-level embeddings are the structural fix. **C1 shipped the read-time seam (2026-07):** representations are chosen per query (`_choose_representation`) and the budget already degrades raw→trusted-summary before dropping — C3's hierarchy (abstract→medium→full, chunks) is *more levels for that same chooser*, not a new mechanism. **C2 shipped the chunk store + chunk-level vector search for documents (2026-07):** `episodic_chunks` + `_vector_chunks` + the shared chunker exist — C3 extends chunking from document turns to ALL long turns and adds the summary hierarchy on top. **Bundle with C17:** the chunk re-embed is the one moment the whole store gets re-encoded — do it at 1024 dims so the store is only rebuilt once.
- [x] **C4 Whole-conversation batch summary** `(planned)` — **Done 2026-07-19 (commit `2c33258`, migration `a7c5e91d3f28`; spec rev notes 1–10 recorded before coding per rule 12).** One evolving summary per conversation in `conversation_summaries` (PK=conversation_id, **ondelete CASCADE — C10's conversation delete needs no extra code**, asserted in test check 6). New [src/workers/conversation_summary.py](../src/workers/conversation_summary.py) `run_conversation_summaries` — session-end burst member (the D1/D2 trio is now a **quartet**) + 7200 s cadence (idempotent per `covers_through`; quiet conversations are no-ops): incremental fold of the NEW turns' C1 representations in bounded ~3.5k-word chunks (must-keep terms via `turn_density`, one coverage retry, ≤250 words), row creation gated on the D3a window condition, LLM failure keeps the old row untouched; `conversation_ids=` restricts a pass (tests never stub-summarize real conversations). Consumers: (a) assembler `conversation_summary_block` — `=== CONVERSATION SUMMARY ===` in the system message for the active conversation once `total_tokens > estimate_recent_window_tokens(...)` (B2's condition, C16-aware), stamped "(as of N turns ago)" when the burst is behind; (b) `_batch_summary_lookup` grew a cross-conversation half over `conversation_summaries` embeddings — excludes the active conversation (the assembler owns it — the double-inject trap), excludes private conversations via the scope join, and is skipped entirely under incognito. Incognito conversations DO get summaries (their own context). T-track era-digest seam preserved: T4 reads these rows as-is. Validated: [tests/test_c4_c9.py](../tests/test_c4_c9.py) checks 1–3 + 6 (28/28 file total).
- [x] **C5 Clustering fix (creation + assignment + merge + retrieval scoping)** `(bug)` — **Done 2026-07 ("v5"), validated 13/13 live-DB** (`tests/test_clustering_v5.py`; LLM naming stubbed like A1/A2/A6 — naming quality pends Z1). Root causes were **creation/assignment-side, not threshold-side** (user widened scope to creation — correct): (1) **exclusive assignment** — v4 linked a turn to *every* cluster above threshold, and each then recomputed its mean centroid including that turn, dragging co-member clusters together until merge glued them: multi-assignment + mean-centroids was the mega-cluster feedback loop; now single best cluster only. (2) **Wait-for-a-friend creation** — no more instant one-turn clusters: unmatched turns wait, get pairwise-grouped (union-find over embedding+entity+same-session scores) and a cluster is born only from ≥2 turns; a lone turn earns a singleton after 24h (retrieval-safe meanwhile: link-less turns pass the cluster filter). (3) **Session affinity** (the C6 payoff): +0.10 when the turn's session_id appears in the cluster — replaces the 2-queries-per-comparison temporal-predecessor bonus. (4) **Noise bonuses removed/capped** (name-overlap bonus deleted — sat at its cap against everything; tag bonus capped 0.10). (5) **Merge finally scheduled** (beat every 3h — was never running, half of G10) **+ singleton re-absorption repair pass** (heals the singletons already in the DB); pairwise merge keeps the conservative v4 gates. (6) **NER-grounded naming** (A2's pattern — recurring member entities injected into the prompts). (7) **Retrieval scoping fixed**: `_relevant_cluster_ids` flat top-10 → **adaptive band** (≥80% of best, capped), and its per-row name/desc `embedder.encode` (up to 30 forward passes on the synchronous path!) deleted. (8) Deterministic oldest-first queue (was LIMIT with no ORDER BY). **Composability (C7/D1 look-ahead):** both tasks are thin wrappers over `run_cluster_assignment(db, conversation_ids=None)` / `run_cluster_merge(db, conversation_ids=None)` — session-triggered per-conversation runs are one call. `cluster_restrict`'s ablation value (+0.66 recency) can now actually cash in; re-measure at Z1/FINAL.
- [ ] **C6 Session IDs + scoping semantics rework** `(rework)` — Introduce a session_id per sitting (turns in one session are likely same-topic ⇒ better clustering; also the hook for decay catch-up, C7). Rethink scopes: `none` should behave like true incognito; `manual` becomes a mode *inside* auto/project rather than a peer; user can toggle extra conversations into scope from the sidebar (cross-chat retrieval over N ticked convos) and `@`-mention a specific conversation/turn for one-off context. **Codex-retrieval side already ready (A5):** `_codex_scope_sets` accepts `scope["batch_ids"]` / `["conversation_ids"]` / `["conversation_id"]` / `["isolated"]` (empty-set = incognito) and every mode above resolves to a batch-id set, so C6's codex work is just **populating `scope` in main.py** — no orchestrator changes. What C6 still owns: the session_id column + assignment, applying the same batch-set scoping to the **episodic legs** (bm25/vector filter by conversation, not yet by the richer scope), the None-incognito wiring (G16), and the UI toggles/@-mention. **BACKEND PRIMITIVE SHIPPED 2026-07 (validated 13/13 live-DB, `tests/test_session_scoping.py`):** `episodic_memory.session_id` (migration `e8f3a26c5d17`) assigned at write time by `src/memory/session.py::resolve_session_id` — same sitting while the gap ≤ `settings.session_gap_minutes` (30, user-confirmed), else a new session is minted and a **`session_started` log event fires (the C7 trigger seam)**; pre-migration rows stay NULL (no guessed backfill). G16 incognito fully wired (see G16 — done), including the wide-net scope leak fix. **What C6 still owns (why unchecked):** populating the richer scope forms (`conversation_ids` cross-chat sets, `batch_ids` @-mention) from the UI — the sidebar toggles and `@`-mention are **Track-F** (see the F-track ledger); the `manual`-becomes-a-mode-inside-auto/project semantics rethink; and episodic-leg support for the multi-conversation set — **SHIPPED EARLY by the E-core (2026-07-18, `fe8df63`): D11 needed it, so the legs now take `scope["conversation_ids"]` via `_conv_scope_filter` (`= ANY(:conv_ids)`, param-driven, single-conv byte-identical); C6-F only has to populate the scope key from the UI.** **S1 note (2026-07-10): no spec needed** — backend shipped; the remainder is F-owed UI (ledger) + that trivial IN-filter; this entry + A5's primitive are sufficient. **Addendum (recovered from rough notes, 2026-07-12): selective memory EXCLUSION** — the notes' scoping section also wanted the *inverse* of the sidebar toggles: exclude a specific conversation or cluster from retrieval **without deleting it** (abandoned projects, sensitive-but-keep content). It fell through every item — C6 only ever *adds* to scope, C10 only deletes. Mechanically it's the same batch-set primitive negated (an exclusion set ANDed as `NOT IN` across the episodic legs + codex scope sets, mirroring the `is_private` invariant pattern) plus a sidebar toggle (F-owed; treat as part of the ledger's C6 line). Build with C6's remaining scope work. (Found by the [BRUTAL_ASSESSMENT.md](BRUTAL_ASSESSMENT.md) §5 sweep.)
- [x] **C7 Decay/worker scheduling rework** `(bug/open)` — **DONE 2026-07-11 (commits aec88a8 + validation/propagation follow-up), validated 49/49 by [tests/test_maintenance_runtime.py](../tests/test_maintenance_runtime.py) + a live two-turn stack run.** Shipped: Celery+Redis **deleted** (celery_app.py gone, redis out of docker-compose/pyproject/.env; postgres is the only external service); in-process **maintenance runtime** ([runtime.py](../src/workers/runtime.py): asyncio tick 60s+jitter, gpu(1)/cpu(2) lanes, event/overdue/work-unit triggers, `maintenance_ledger` schedule state + duplicate-instance lease, 30/120/480s backoff) started via [core.py](../src/api/core.py)`::create_core()` (HTTP-free — the E7 headless seam); all workers are plain callables (decay family takes `cycles`, applied as `rate**cycles`); post-flight chains chunker/codex/procedural as direct calls with per-stage idempotency; session-gap work-unit wired from `resolve_session_id` (commit/task_done reserved for E3/E4 via handler registration); session-end burst runs reflection+batch_summarize+the D6 consent-gated fine-tune check (weekly crontab dead); shared-first bg model (mode=shared default, per-turn jobs use the exact serving model via `model_used`, `background_model_name` pin available, vLLM demoted to manual dedicated path); SESSION_STATE→`conversations.sticky_model/consecutive_shifts` (D9/G8); G2/G4/G10/G12/G18/G20.6/G20.7 settled in passing. **Four spec corrections recorded in the spec ([rev 2026-07-11] markers), biggest: decay catch-up cycles derive from the job's own ledger row, not the chat gap — the specced gap-based formula double-decayed whenever the app stayed open.** Validation: pure-logic (overdue math, cycles clamp, single-flight, lane serialization, retry→error, tick crash isolation, work-unit table, fine-tune gating) + live-DB (post-flight chain with stubbed LLM, decay `cycles=4` == rate⁴ under a rolled-back transaction, ledger lease, session-end burst) + live run (full pipeline in-process on shared Ollama, overdue catch-up, session-gap freshening, G26's CL7 check). **Look-ahead honored:** callables keep names/signatures (D1 agent + FINAL harness), `create_core()` is E7's boot entrypoint, work-unit handlers are E3/E4's seam, ledger+structlog events are F5's data source, `./ice`/`stop_ice`/`setup.sh` changes kept thin per the Track-F end-state. *(Original entry below for history.)* All decay depends on the app staying open; a user who closes it gets no maintenance. Run catch-up at session start based on gap since last session (msgs + elapsed time + calendar spread of mini-sessions). Decay is cheap enough for session-start; **open question:** where to put fine-tuning, which is expensive even on strong hardware. And for the rest of the background workers a way where once a session is over all the other bg workers run to do their work but based on the similar logic of decay, so we dont over do it or under do it. **Unified framing (2026-07, Track E decision 5):** "a unit of work finished → reconcile memory" is one concept with two triggers — **session-gap** here (conversation has no commit, so the C6 time-gap session boundary is the signal — **the hook already exists**: `resolve_session_id` fires a `session_started` log event on every new sitting; C7 turns that into the catch-up trigger) and **git commit / task-done** on the coding side (E3/E4). Design C7's catch-up runner so the coding-side reconcilers can hang off the same trigger machinery. **Explicit decision point added by the 2026-07 audit: does Celery+Redis survive into the packaged product?** For a single-user local app, a broker + beat + result backend + 13 distributed workers is heavy machinery for what is functionally "run maintenance jobs when idle/triggered" — an in-process scheduler (APScheduler/asyncio) would remove two services and a whole failure class (the G21 boot crash is the proof-of-fragility). Don't migrate mid-development; **decide here**, since C7 reworks scheduling anyway and Track F's end-state ("scripts go away, one packaged app") is the forcing function. **G18 (Celery result backend) is folded into this decision** — invest nothing Celery-specific until it's made. **→ spec: [docs/specs/C7_scheduling.md](specs/C7_scheduling.md) (S1 2026-07-10 — DECIDED: Celery+Redis replaced by an in-process maintenance runtime in the core process; triggers (event / overdue / work-unit), closed-form decay catch-up, shared-first implementation (G2/G4/G12 folds), H5 consent-gated fine-tune, G8-without-redis, and the full deletion list are all specced.)**
  - **SETTLED (user, 2026-07): SHARED-FIRST** — the dedicated vLLM bg server leaves the default stack; bg model defaults to the chat model; dedicated demotes to a power-user config behind the `bg_client_factory` seam; G2 folds in, G3 parks. Full rationale, the "shared but better" details, and the Z1 watch-item live in the C7 spec (D7).
- [x] **C8 Time-weighted episodic retrieval** `(planned — F9)` — Done 2026-07, validated in `tests/test_c8_c15.py` (identical embeddings: 1h-old turn scores 1.250 vs 1.012 for a 90-day-old one; creative gap = 0). Recency now lives **inside the vector SQL score** — `× (1 + 0.25·exp(−age_days/30))`, A11's codex formula, gentler because episodic keeps its post-fusion bonuses — applied to the turn-level vector leg, the C2/C3 chunk leg (parent timestamp), and the wide net. **Why in-score, not another bonus:** the candidate set is `LIMIT 100` by score; post-fusion bonuses can't rescue a recent turn that never made the cut — this fixes *candidacy*. **Creative-skipped** (same rule as `_apply_bonuses`: recent meta turns are noise for narrative recall).
- [x] **C9 Procedural memory + slots rework** `(rework)` — **Done 2026-07-19 (commit `2c33258`, migration `a7c5e91d3f28`).** **Procedural:** the 3-intent whitelist is deleted from `_procedural_lookup`; the leg always runs and precision = embedding rank (LIMIT 5) + `_procedural_trigger_match` (kept — the trap honored) + the new SQL floor `confidence_score ≥ settings.procedural_min_conf` (0.3; the Z1 flood rule is in the spec). Extraction-side audit (spec D4): post-C7 the dispatch has NO intent whitelist — nothing to kill (rev note 4). **Root-cause bonus — the leg was FULLY dead, not just gated:** its embedding bind lacked the PgVector type, so every call raised `vector <=> double precision[]` → rollback → `[]`; the intent gate had been hiding a 100%-dead leg (the "+0.02 in Exp2 / nobody ever felt it" result is now overdetermined). Fixed here, plus the identical latent bug in `_rag_lookup` (in-passing boy-scout, logged in CLEANUP.md). **Slots:** three tiers via `scope_tier`/`project_id`/`conversation_id` + a NULLS-NOT-DISTINCT unique index (`memory_slots` had NO name uniqueness before — the old `initialize_slots` docstring claimed one that never existed); `VALID_SLOTS` is now the per-tier dict in [src/services/slots.py](../src/services/slots.py) (global keeps the 7; project = {project_context, conventions, pending_items, guidance}; conversation = {conversation_focus, pending_items}); per-tier validation names the missing attachment; **G14's 300-token hard cap folded in** (`SLOT_TOKEN_CAP`, truncation flagged in the response); D6 assembly order global → `[PROJECT · X]` → `[CONVERSATION · X]` under PERSISTENT CONTEXT, project slots keyed off `scope["project_id"]` — coding conversations inherit them (the E1 payoff); D7 agentic updates ride the review queue: `review.approve`'s slot arm re-routes through the slots service (tier-validated + capped) and records the PROPOSER (`proposed_by` → `updated_by`: reflection/agent); `ice_slots` grew `tier`/`project`/`conversation_id`, the REST router grew tier params + response fields, `session_start_block` lists the global tier only. Validated: test_c4_c9 checks 4–5; regressions services 48/48 (review section updated to the D7 contract), coding-core 46/46, mcp 21/21, runtime 49/49, agent 43/43, timescope 61/61, session-scoping 13/13, reconcile-on-read 16/16, memory-decision 31/31, smoke 70; migration round-trip. **Look-ahead:** C11's chat commands + F2/F4 panels are adapters over the same slots service (tier params ready); C10's per-conversation delete inherits the slot FK.
- [x] **C10 Deletion feature** `(planned)` — **Done 2026-07-19 (commit `e3f24cb`; spec rev notes 1–15 recorded before coding per rule 12; NO migration — 'stale' is a free Text status).** New [src/services/conversations.py](../src/services/conversations.py)`::delete_conversation(db, conv_id, dry_run)` — **manifest-first**: phase A pure reads build the per-store manifest + the exact work lists, phase B applies them FK-ordered in ONE transaction; `dry_run=True` runs phase A only, so the preview is provably identical to a real run (asserted). Cascade: live conversation-source edges split **corroborated** (an `edge_added` event with `batch_source` OUTSIDE the deleted batches — rev 2 fixed the literal "≥2 distinct" reading, under which a same-conversation double-extraction would immunize its own edge) vs **sole-support** → `_expire_edge(reason="source_deleted", source="user_deletion")`, journaled and **excluded from T4 timelines** (evolution.py already dropped that reason since T4 — verified, no change owed; check 4 asserts it); eventless legacy edges count sole-support (spec §3 decision). Orphaned conversation-source entities with no user-authored description (no mcp_edit/manual_edit `description_updated` event) are **husked** D1/D2-style (rename marker, aliases+embedding cleared, `entity_expired` journaled; never hard-deleted); surviving touched entities get `_regenerate_context_payload`. Sweep: turns (chunks CASCADE) + links + empty clusters (born-here survivors detached) + cold rows + batch/**conversation (C4)**/**session** summaries + replays + **conversation-tier slots (C9)** + curated labels; procedural arrays pruned (`&&` via uuid[] cast — the generic-ARRAY column has no `.overlap()`), emptied → `is_active=False`; **live `decisions` with `source_batch ∈ batches` expire bi-temporally** (E-core table the b8c2122 grounding predates); pending review items referencing the conversation → `status="stale"` (codex_reconciliation via the old edge's source_batch, decision_supersession via the decisions' source_batch, forget_request via origin/listed turns). Mid-generation refusal via the new `runtime.generation_in_flight` property (in-process; the MCP process can't see the app's stream — accepted single-user reality). The manifest restates the **G25 logs caveat verbatim** (logs are NOT touched — honesty over pretense). Adapters: `DELETE /user-control/conversations/{id}?dry_run=` (409 mid-generation) + `ice_control delete_conversation` (tool description instructs: ALWAYS preview and get the user's explicit go-ahead) / `forget_propose`. Validated: [tests/test_c10_c11.py](../tests/test_c10_c11.py) checks 1–4 + manifest-count equality + mid-gen refusal (56/56 file total; fixture conversations only, explicit `edge_added` events, never truncates). **Look-ahead honored:** F2 renders manifests/proposals as-is (self-describing dicts); B6's branch deletion reuses this cascade per-branch (lineage = conversation set); G25 owns log redaction — the caveat string names it.
- [x] **C11 User memory control through chat** `(planned)` — **Done 2026-07-19 (commit `e3f24cb`, same session as C10).** New [src/api/chat_commands.py](../src/api/chat_commands.py)`::try_handle(db, runtime, conv, text, scope)` — **adapter #3 over E0's services exactly as promised** (parser + rendering only; every handler a short hop into slots/bookmarks/scoping/retrieval_svc/conversations). Wired in `chat_completions` AFTER the G26 conversation/scope resolution (needs the conv row; /search gets main.py's scope dict verbatim — chat-path parity incl. incognito rules and the E11 project freshen) and BEFORE `classifier.classify` (commands never burn pre-flight latency). Handled commands **short-circuit the LLM entirely**: the confirmation streams as a minimal OpenAI `chat.completion.chunk` SSE sequence (`model="ice-commands"` — any frontend renders it), and the turn is NOT stored (no episodic row, no post-flight; the journal is the record — a stored "/slots" would pollute retrieval). Unknown `/x` → help hint, **never silent fallthrough**; only the first line parses, the rest is payload; `in <slot>` strips only when it names a valid slot for the tier ("check in tomorrow" stays text). **v1 set (D4):** `/remember <text> [in <slot>] [@project|@conversation]` (tiered append; G14 truncation surfaced in the confirmation — the live pre-G14 pending_items slot is 31k chars, so this fires in practice), `/slots [tier]`, `/bookmark` (last *stored* turn; friendly refusal on empty), `/search` (context_for; dated source-tagged fragments, **zero LLM** — test booby-traps the bg client), `/scope auto|project|none` (passes existing cluster/filter state through — a bare `/scope` never wipes assignments), `/forget` (**fuzzy + destructive ⇒ queues** a `forget_request` proposal: top-5 embedding-matched turns, visibility-guarded, PgVector bindparam + name-containment edges; zero matches queues nothing; NEW `review.approve` arm applies via `conversations.apply_forget` — turns deleted, edges expired journaled `user_forget`, curated labels cleared, vanished rows tolerated), `/delete-conversation` (dry-run manifest → `confirm` within a 10-min process-local TTL; double-confirm = idempotent refusal), `/help`. Every applied command journals `updated_by='chat_command'` + one `chat_command` structlog event (the F5 telemetry stream beside `mcp_tool_call`; per-command counts feed the F discoverability conversation — the empirical deferral stands). Natural-language commands stay v2 (false-positive risk; MCP covers harness-side conversational control). Validated: test_c10_c11 checks 5–9 (56/56 file total); regressions services 48/48, mcp 21/21, c4_c9 28/28, session_scoping 13/13, smoke 72. **Look-ahead honored:** F owns discoverability/affordance (ledger line updated); F16's deep-search rides this parser as a new command; commands stay ≤5-line hops so F1/F2 inherit every operation.
- [ ] **C12 RAG leg completion** `(planned)` — Proper document pipeline (PDF, CSV, …), per-conversation uploads, and a sidebar way to add an existing document into another conversation's scope. **Not plain RAG chunking (user):** an uploaded document should be *run through ICE's own machinery*, not just split into vector chunks and dumped in a `rag_chunks` table. Treat ingestion like the system living through the content (the F10 "system lives through the logs" philosophy applied to docs): extract Codex entities/relations from the document (so a PDF's people/systems/facts become first-class graph nodes with backlinks), run clustering so its sections join the topic structure, apply the C1 lossless-vs-summary density decision + C3 hierarchical summaries per section, and let the document's knowledge be retrieved through *all* legs (codex graph, clusters, procedural) — not only the isolated vector-RAG leg. Net: a document becomes real ICE memory the graph and clusters can reason over, not a searchable archive bolted onto the side. Design against C2 (chop-at-ingestion, inject only relevant chunks) and C3 (chunk-aware retrieval) so the same chunking/decomposition primitives serve both pasted big-inputs and uploaded files. **Also serves E10:** project-internal docs (README, docs/, dropped specs) go through this same pipeline under the project's scope — C12 is the ingestion engine for coding-side documentation too. **S1 note (2026-07-10): no separate spec** — this entry is decision-complete as written (ICE-machinery ingestion over the C2/C3 primitives + F10's engine, E10 scope); implement at flow phase 8; the upload UI is F-owed.
- [ ] **C13 Caching strategy** `(open — premature, not blocked)` — Never designed at all. Decide what to cache (embeddings, cluster scores, classifier outputs, hot fragments) and whether Redis or in-process. **Why wait (2026-07):** nothing external blocks this (unlike C12/C6-UI), but a caching *strategy* is cross-cutting and its hard part — **invalidation** — couples to write-paths still being reworked: **cluster scores** (C5 clustering is changing), **retrieval fragments** (C1/C2/C3 change fragment shape/content), **classifier outputs** (B1 retrain changes them), and decay/codex writes already invalidate vector-score/codex-payload caches. Design it now and you write invalidation twice. Also there's **no measured bottleneck yet** (profile at Z1), and it overlaps **C14** (KV-cache), which wants a settled prompt/fragment shape. **Not all-or-nothing, though:** the *stable* pieces can be pulled forward opportunistically without the full strategy — caching the frozen-encoder embedding for repeated prompts, and the `nvidia-smi` GPU poll (already scoped in **G4**). Do the full strategy after ①/② stabilise the write-paths. **→ spec: [docs/specs/C13_C14_caching.md](specs/C13_C14_caching.md) (framework: two invalidation-free pieces ship with Z1-prep; everything else behind Z1-profile decision rules; fragment caching presumptively rejected).**
- [ ] **C14 KV-cache persistence & cache-aware retrieval** `(open — long-term)` — Stable-prefix ordering is right but empirically only ~10–15% of the prompt is cacheable today. Investigate persistent prefix KV storage, cache-aware fragment selection, incremental prefix updates — depends on backend (Ollama/vLLM/SGLang) cache APIs. **→ spec: [docs/specs/C13_C14_caching.md](specs/C13_C14_caching.md) (investigation protocol at Z1; no build).**
- [x] **C15 Wide-net fallback budget + honest trigger** `(bug)` — Done 2026-07, validated in `tests/test_c8_c15.py` (6 pure checks). (1) **Budget:** the hardcoded 2,000-token ceiling is gone — wide net now takes `max(1500, 0.3 × max_retrieval_tokens)`, so it scales with the C16 model-aware budget while staying deliberately tighter than normal retrieval. (2) **Trigger migrated off the legacy `max_confidence`** (max over all 25 probs — dominated by whichever head was peaked): `_head_confidences` splits topic/intent maxima from `raw_probs`, and the wide net fires only when **both** heads are weak (< threshold) — it's a degraded single-leg mode, so a peaked topic with a fuzzy intent no longer trips it, and a genuinely lost classification still does. DI3 fast-path results (all-zero probs) fall back to their explicitly-set confidence.
- [ ] **C16 Model-aware context budget** `(new — raised by user 2026-07; MODEL-AWARE HALF DONE 2026-07)` — ~~`TOTAL_CONTEXT_BUDGET = 23_000` hardcoded and model-blind~~ **Shipped, validated 8/8 (31/31 total in `tests/test_memory_decision.py`):** model selection now happens **before** budgeting/retrieval in main.py (resolving the ordering seam — and deleting G20's duplicated selection blocks *plus* a stray `ollama_url` override that had silently killed registry `base_url` routing); `derive_total_budget` (memory_decision.py) computes `context_window × context_input_fraction` clamped to `[context_budget_min, context_budget_max]`, falling back to `context_budget_fallback` when the model is unknown to the registry (`get_model_context_window`); flows through `set_budget_from_turn_count(total_budget=…)` (configurable_orchestrator updated in lockstep — G19 drift avoided) and the B2 memory-pressure window estimate. All knobs in settings. An 8k model now gets ~6k, a 32k model ~24k, a 128k model the max guardrail. **Still open (the smart half):** *need-based filling* — the ceilings are guardrails, but inside them filling still uses the turn-bracket `growth_cap` formulas; replace fill-to-cap with **marginal-relevance stopping** ("this much is enough": score-gap / knee detection over the fused ranking + per-leg diminishing-returns cutoffs). That's the proper fix for the ablation's dynamic_budget −0.11 (the feature wasn't wrong, the fill-to-cap policy was), and what makes raising/removing the max guardrail safe. Mode-awareness (coding pointers are cheaper per fact) lands with Track E. **Need-based, not formula-based (user, 2026-07):** the current growth curves (`growth_cap` by turn-count brackets, recent-fraction by brackets) are hardcoded formulas — turn-based/token-based expansion "isn't smart." Redesign: the model-aware ceiling is a **guardrail, not a target** — inside it, filling stops on **marginal relevance** ("this much is enough"), e.g. score-gap / knee detection over the fused ranking + per-leg diminishing-returns cutoffs, instead of filling to a bracket-formula cap. Keep the hard internal ceilings as safety; make the *decision to stop* content-driven. (This is also the proper fix for the ablation's dynamic_budget −0.11 — the feature wasn't wrong, the fill-to-cap policy was.)
- [x] **C17 Embedding un-truncation: 384 → 1024 native** — **DONE 2026-07-19 (commit `d4d0a79`, migration `b6e2f9a41c73` on top of `a7c5e91d3f28` — the new alembic head; validated in `tests/test_longevity.py` 26/26 + the regression sweep in G23's note).** ONE revision: `store_meta` created; all NINE vector columns (episodic_memory, episodic_chunks, context_clusters, codex_entities, procedural_memory, decisions, rag_chunks — with the NOT-NULL drop/conditional-restore dance — batch_summaries, conversation_summaries) altered to `vector(1024) USING NULL`; the canonical **HNSW cosine index set created for the first time** (the live DB had NO vector indexes — G6's orphan `scripts/database/create_indexes.sql` was never applied; indexes now land through migrations); stamps seeded pending-if-rows / done-if-empty. Store cutover: archive → **user-approved wipe** → migration over the empty schema (the runner's live run was trivially instant; the machinery is behaviorally validated on seeded fixtures — checks 2/5/6). Consumers: retrieval + every writer now native-1024 through the shared embedder; **A4's `_unit` re-normalization workaround DELETED** (native output is unit-norm; `_match_entities_by_similarity`'s "already normalised" comment was false for the whole 384 era and is true now); classifier + micro-NER consume `slice384()` — **empirically BIT-IDENTICAL to the old truncate_dim output** (ST 5.5.1 truncates after Normalize with no re-norm; proven in check 7) — until B1/A9 retrain; `fine_tune.py`/`dataset.py` slice the same way so training features keep matching the live head. `test_retrieval.py`'s `all-MiniLM-L6-v2` relic (the original source of the whole 384 shim) replaced with the shared embedder. Retrieval-quality delta at 1024 is measured at Z1/FINAL as planned (expectation stays "modest; chunking was the big win").

## Track T — Temporal retrieval & idea evolution (the exocortex layer)

> **Status 2026-07-12: TRACK T COMPLETE — T1–T3 shipped in commit `c18165c`, T4 in `850f10d`; all 30 spec checks green (61 assertions in `tests/test_timescope.py`).** The exocortex queries that motivated the track ("what was that idea two years ago", "my ideas from H1-2025", "how did X change") are now structurally answerable; FINAL pt. 5's temporal probes will measure how well. The origin note below describes the pre-T world — kept as the historical record of why the track exists.
>
> **Origin (user reflection, 2026-07-10 — the paper-vs-vision gap).** ICE's original purpose was an exocortex — "what was that idea I had two years ago", "my ideas about X from the first half of 2025", "what did I think about X at time T and how did it change" — and the system as built cannot answer any of these. The audit that produced this track found the gap is **query-side only**: storage has THREE temporal layers — `EpisodicMemory.timestamp` (+ C6 session_id), bi-temporal `CodexEdge.valid_from/valid_until` with real supersession writers (A6 reconciliation, A8 negation-retraction, property updates), and a full **event-sourcing journal** (`CodexEvent`: every edge_added/edge_expired/property_updated per entity, timestamped, with batch provenance; `CodexSnapshot`: materialized state anchors) — and **retrieval reads none of them for time**: all four codex traversal sites hardcode `valid_until IS NULL` ([orchestrator.py](../src/retrieval/orchestrator.py) 1079/1130/1317/1320), the episodic legs have no timestamp predicate at all, nothing anywhere parses a time expression, no intent label is temporal, and every shipped temporal feature (C8, A11, `_recency_bonus`, decay→`cold_storage`) points at NOW — the arrow of time is one-directional, and the oldest memories literally leave the searchable store (`cold_storage` has no retrieval reader). The smoking gun: [prompt_assembler.py](../src/api/prompt_assembler.py):143 *instructs the model* to narrate how facts changed over time and cite timestamps — while fragments render undated, codex facts render current-only, and the prompt doesn't even contain today's date. The model is asked to do temporal reasoning and given no temporal data. The experiments never caught this because no probe asked a time question (FINAL pt. 5). The symptom in Exp2: vector-RAG answered the saga-ending question with the OLD draft, full ICE with the NEW one (recency features working as designed), neither with the evolution — the evolution answer is structurally impossible today.
>
> **Git as the mental model (user) — the plumbing exists, the porcelain doesn't.** The user independently envisioned the codex working like git's object model. Code verdict: that machine is already built, in DB idiom — `CodexEvent` ≈ the commit log (append-only, provenance ≈ author, the source turn ≈ commit message), `CodexSnapshot` ≈ tree snapshots, bi-temporal edges ≈ never-overwritten history; compaction is already **lossless** (marks `compacted`, never deletes — verified in [compaction.py](../src/workers/compaction.py)). Missing is git's *porcelain*: **checkout** (read the graph as-of T → T3), **log** (entity/idea timeline → T4), **diff** (`entity_diff(t0,t1)` → T4). Deliberately NOT adopted: content-addressing (distributed-sync integrity machinery, pointless on a local DB) and **branching** (parallel co-true idea versions — linear supersession + coexisting non-conflicting edges + A8 negations + E8 `alternatives_rejected` cover the real cases; revisit only with B6). Adopted discipline: **never overwrite content** — `CodexEntity.description` is today mutated in place unjournaled (A7.3 enrichment / manual edits silently destroy the old note); T4 journals it.
>
> **Sequencing & seams:** T1 is tiny and standalone (candidate to implement in-window). T2→T3→T4 build on each other. Mode-aware recency *extends* C8/A11's formulas (same exponential, movable origin — don't fork the legs). D1's agent later inherits history maintenance; G10 now carries a lossless-compaction constraint; C4's conversation summaries **(shipped 2026-07-19, `2c33258` — `conversation_summaries` rows are live)** are the cheap era-digest source for T4's stratified sampling (T4's consumption of them remains future work); E4's git-history replay can share T4's timeline renderer; F3 gets `entity_diff` as its timeline-overlay backend. Spec: [docs/specs/T_temporal.md](specs/T_temporal.md) (S1, 2026-07-10).

- [x] **T1 Date-grounding the prompt** `(new — bug-adjacent; smallest, do first)` — **Done 2026-07-12** (commit `c18165c`; spec §2.8). Shipped: (a) `Today's date: YYYY-MM-DD` opens the system prompt + a fragment-dating instruction sentence ([prompt_assembler.py](../src/api/prompt_assembler.py)); (b) every episodic/chunk/cold fragment prefixed `[YYYY-MM-DD]` in `_rows_to_fragments`/`_vector_chunks` — degrade/abstract forms keep the stamp; (c) codex fact lines render `(since YYYY-MM)` from `valid_from` via the shared `_fact_line` helper (month precision; legacy NULL undated; also fixed negated edges rendering as positive facts) — the "superseded 2026-05" half is T4's timeline renderer as planned; (d) recent-window turns stay undated; batch summaries get `[summary, date]`. **Grounding correction found mid-implementation (recorded in the spec's `[rev 2026-07-12]` block):** the entry's claim "timestamps already flow through the leg SQL" was false — no episodic SELECT carried `timestamp` since pre-experiment `616d770`, which also means the `_rows_to_fragments` recency tiebreakers (~1826/~1832) were dormant dead code through all experiments; T1 re-added `timestamp` to the four SELECTs and **removed** the dead tiebreakers (awakening them would have been an untuned scoring change). Validated: checks 15/17 + regression check 30 of `tests/test_timescope.py` (text = old text modulo date prefix; old/new recency formula numerically identical at center=now).
- [x] **T2 Temporal query understanding (TimeScope)** `(new)` — **Done 2026-07-12** (commit `c18165c`; spec §2.1–2.2). New [src/retrieval/timescope.py](../src/retrieval/timescope.py): pure, LLM-free, <1 ms — expression grammar (absolute months/quarters/halves/seasons/years/dates, relatives incl. number-words, since/before/between wrappers, longest-match-wins overlap resolution) + evolution cues + the **joint gate** (resolvable expression ∧ recall shape: `?` / interrogative opener / `reference_signal` / p_ltm ≥ 0.5 — a bare date-word in a statement stays `current`, validated by check 7). Guards: future→current, code-stripped, month-without-year→most recent past, vague pasts never resolved, windows clamped to now; bare years/months/seasons require an anchor word (guards "spring cleaning", "march forward"; vs/compare anchor the "2024 vs 2025" case). Threading per D1: `scope["timescope"]` (iso dict via `to_scope_dict`/tolerant `from_scope`), `timescope_detected` log in main.py, `ltm_bump_timescope=+3.0` kwarg-bump in `decide_memory_retrieval` (B2-idiomatic, breakdown telemetry, no hard override — check 18), `_resolve_timescope` gate in the orchestrator (kill-switch `timescope_enabled` + `timescope` ablation flag in ConfigurableOrchestrator, the G19 lockstep edit). **B1 propagation stands:** a temporal-recall label joins B1's bundled retrain; `evidence` tuple is the seam. Detector validated by checks 1–11 (all spec example windows exact).
- [x] **T3 Time-scoped retrieval (as-of & range) + cold-storage second chance** `(new)` — **Done 2026-07-12** (commit `c18165c`; spec §2.3–2.6 + §2.9–2.11; migration `e5b8c2d4a917`). Shipped: (a) window predicates on BM25 (both queries)/vector/chunks/wide-net via `_timescope_leg_filters` (param-driven, one query text per leg — no SQL fork); archived rows visible + decay floor dropped under non-current modes (D10; the floor drop in the *main* legs is spec rev note 3 — archived rows sit below 0.1 by construction); (b) **mode-aware recency** via `_recency_params` — one `EXP(-ABS(t - center)/τ)` formula, center=now (current, numerically identical for past rows), window-midpoint with widened τ and creative included (as_of), flat (range/evolution); A11 edge recency re-anchored in `_edge_trust`; post-fusion now-bonuses gated off; (c) the four `valid_until IS NULL` sites → **`_edge_valid_filters()` valid_at(T)** with legacy-NULL-valid_from passing; evolution navigates the current graph (D5); (d) **entity-state-at-T needs no snapshot replay in v1** — per spec §2.4 decision (spec > entry), valid_at(T) edge filtering IS the state reconstruction retrieval renders; `CodexSnapshot` replay deliberately deferred until a field-level consumer exists (F3); (e) **cold leg `_cold_lookup`** (windowed-only, keyword-or-browse, conversation/privacy scoping mirroring the live legs, LIMIT 5) + **probation resurrection** `_resurrect_cold_hits` (budget survivors only, ORIGINAL timestamp, decay 0.12 = `timescope_probation_score`, runs after `_strengthen_retrieved` so probation starts exactly at 0.12; legacy NULL-conversation rows cite-only; ON CONFLICT keeps the cold row); cold rows now carry `conversation_id`/`is_private`/`batch_id` (D12); (f) **honest emptiness** `_append_empty_window_note` (names the window + nearest eras from an unwindowed probe, never widens). Also: **D11 decay freeze fix** in [decay.py](../src/workers/decay.py) on top of C7's cycles logic (archived rows decay again, symmetric un-archive clause, cold move carries the new columns — cold storage was literally unreachable before this; migration counted 0 rows, confirming). Procedural gets an observation-span overlap filter; batch summaries return [] under non-current modes (D14). Validated: checks 12–14, 16, 19, 25–29 (+ suites: c8_c15, session_scoping, density C1/C2/C3, smoke all green).
- [x] **T4 Evolution surfacing — git log & diff for ideas** `(new — the vision item)` — **Done 2026-07-12** (commit `850f10d`; spec §2.7). Shipped: (a) new [src/retrieval/evolution.py](../src/retrieval/evolution.py) — `build_entity_timeline` (groups an entity's edges into (relation, direction) supersession chains; one month-precision line per member with span, `NOT` for negated, and the expiry reason from the event payload; most-recent `max_transitions` kept with an "(earlier history omitted)" marker; word-trimmed to `timeline_max_tokens` by dropping oldest lines; returns None when no event-backed expired member exists — pure-live timelines would just repeat the note), `entity_diff` (added/expired/retracted in [t0,t1), raw datetimes + other-endpoint names — E0's service candidate and F3's overlay backend, deliberately unwired from any endpoint), `history_exists` (cheap EXISTS gate), `log_description_update` (D13); **the D6/C10 discriminator lives only in this module** (`_expiry_events`: event-backed expiries minus `source_deleted`) — re-verified at implementation: codex_decay emits no events, all five `edge_expired` writers are in codex_extractor. (b) `_codex_graph` wiring: one timeline fragment per history-carrying anchor **in any mode including current** (the always-on saga path), 0.9× the anchor's score, capped 2 (4 in evolution mode), windowed by the active TimeScope; `retrieve()` splits timelines into their own legs-dict entry so RRF weighs them at the pinned `blend_weights["timeline"]=0.6` and the A10 budget gives them their own round-robin lane (`source_type="timeline"`). (c) Era-stratified `_vector_episodic` under evolution mode: candidate LIMIT 100→300 (param-driven, same query text — G19), `_stratify_by_era` cuts by-timestamp into 4 equal-count buckets keeping top-3 by score each (row-shape-agnostic per the C4 look-ahead). (d) D13 journaling at both real write sites — reflection enrichment (**replaces** its `context_appended` emit) and the inject watcher's update path; **grounding correction (spec rev note 5, rule 12):** user_control.py has *no* entity-edit endpoint at HEAD (its only description writes are ContextCluster create — outside D13), so the entry's "user_control" expectation was corrected in the spec; a future manual editor owes `source="manual_edit"`. This also completes T1(c)'s second half: superseded facts now render with their supersession month via the timeline lines. **Validated: checks 20–24** (14 new assertions in [tests/test_timescope.py](../tests/test_timescope.py), 61/61 total incl. the full T1–T3 regression; c8_c15 / session_scoping / density_c3 / smoke re-run green). **Look-ahead honored:** evolution functions are orchestrator-state-free (db + entity in, data out) for E0/D1; no REST endpoints; timeline renderer shareable by E4. *(Original intent below for history.)* — **Context-side, not answer-side (user decision 2026-07-10): provide the evolution info, don't force the narrative** — the model gets the data and the autonomy; the existing prompt instruction decides how much to say. Two provisioning paths: (i) **always-on supersession enrichment** — supersession "happens ALWAYS", not just under temporal phrasing (the saga question had none): whenever a retrieved anchor entity carries relevant expired/superseded/negated edges, attach a **compact timeline fragment** (one line per transition: date, was→became, `CodexEvent` reason, source-turn pointer as the "commit message") so any answer *can* open with "originally X → revised to Y" when the model judges it relevant; (ii) **explicit evolution mode** (TimeScope from T2) — the full treatment: edge history ordered by `valid_from`, **`entity_diff(entity, t0, t1)`** (added/expired/retracted between two times, derived from the journal — git-diff for the graph, later F3's timeline-overlay backend), and **era-stratified episodic sampling** (deliberately sample the idea's lifetime, not pure top-k; C4 summaries become era digests when built). Timeline fragments are a first-class fragment type under A10's round-robin fairness with a hard cap (a life story must not eat the window). Plus the never-overwrite fix: **journal `description_updated`** (old→new) so note bodies replay like edges. **Look-aheads:** D1's agent inherits history maintenance; G10 compaction stays lossless (constraint recorded there); E4 shares the timeline renderer; branching stays out (see preamble).

## Track D — Active ICE (agentic background maintenance)

- [x] **D1 Memory Maintenance Agent** `(new)` — **Done 2026-07-17 (commit `7cc02b0`, with D2; spec re-grounded at `126db2d` first — six refinements recorded as the [rev 2026-07-17b] note per rule 12).** Shipped [src/workers/maintenance_agent.py](../src/workers/maintenance_agent.py): **deterministic worklist + bounded LLM decisions, NOT a free tool loop** — five SQL detectors in a pluggable `DETECTORS` registry (1: `codex_reconciliation` leftovers re-decided WITH both edges' source turns, `agent_attempts` capped at 2, settled items → `status="resolved"`; 2: pending-edge pileup — the sentinel's one real query ported, thresholds from its seed rule, plus a live-pending clause so fixed entities stop re-detecting; 3: duplicate-entity candidates — casefold name/alias intersection ∪ pgvector cosine ≥0.90 same-type, top 10, name-equal ⇒ Tier 0, cosine-only ⇒ Tier 2 with `same`/`different`/`unsure` verdict, rejected/decided pairs skipped forever via sorted `pair_key`; 4: contradiction backlog — positive+negated coexisting or two live antonym edges → deterministic newer-supersedes via A6's `reconcile_conflict`; 5: stale `pending_items` slot → LLM content suggestion as a `memory_slot_update` proposal). Three risk tiers; caps 50 scanned / 25 LLM / 10 applications / 5 proposals per run; decisions via one bg-model JSON-mode completion at temp 0, **unparseable/out-of-enum ⇒ unsure ⇒ no write**; `llm_decider=None` degrades gracefully (Tier 0 + deterministic Tier 1 still run). **D5's real `merge_entities` replaced the codex_ops stub:** re-point all edges (live dups expired-journaled, survivor keeps max strength/confidence + earlier valid_from; pair-edges expired, not self-looped), union aliases/tags/properties, longer description wins (journaled), events moved, payload regenerated, absorbed row kept as a renamed husk (`[merged:<id8>]` + `properties.merged_into`) — **nothing hard-deleted**; the E0 review-approve dispatch now runs it for real. Full audit: `agent_run_id` on every action, CodexEvents with `source="maintenance_agent"` + `batch_source=agent_run_id` (G17 partial), `agent_action`/`agent_run_summary` structlog events (D3-roadmap discharged; F5 promotes). Scheduling: gpu lane, overdue 12h + session-end burst. Seam extensions: `_expire_edge`/`reconcile_conflict` optional `source=`, `log_description_update` optional `batch_source=`. **Validated 43/43** ([tests/test_maintenance_agent.py](../tests/test_maintenance_agent.py) — all §5 checks 1–10 incl. Tier-0 merge end-to-end, Tier-2 proposal→approve→real merge, rejected-pair skip, all three caps exactly, unsure-no-write, sentinel-gone); regressions green (runtime 49/49, services 48/48, parity 31/31, smoke 63/63). **Look-ahead honored:** detectors are a typed-item registry E1's decisions table plugs into; `run_maintenance_agent`/`merge_entities` stay router-free (E0 rule); proposals render from `item_content` alone (F2). *(Original entry below for history.)* A small tool-using agent (3B/4B, guided decoding) running in idle GPU time… **→ spec: [docs/specs/D1_D2_maintenance_agent.md](specs/D1_D2_maintenance_agent.md) (S1 2026-07-10 — worklist + risk tiers + bounded decisions; includes D2's sentinel removal and the review-queue apply-on-approve fix.)**
- [x] **D2 Sentinel subsystem: fold into D1 and REMOVE** `(rework → removal)` — **Done 2026-07-17 (commit `7cc02b0`, with D1).** `sentinel_monitor.py` deleted; `SentinelRule`/`SentinelEvent` models + tables dropped (migration `f7a3d9c21e46` — archived the 3 seed rules into the migration log first; downgrade recreates the shapes, validated round-trip); runtime job + `sentinel_monitor: 1800` interval replaced by `maintenance_agent: 43200`; `review.py`'s no-op `sentinel_review` arm removed (behavior identical); `scripts/seed_sentinel_rules.py` → `scripts/oneoff/`; the `TRUNCATE` list in `tests/test_full_pipeline_phase_9.py` updated. Its two real checks live on as agent detectors 2 and 5 — nothing else was worth porting (the audit verdict held: stubs everywhere). *(Original entry below for history.)* **Do NOT complete the Sentinel.** Code evidence it never earned existence: of its trigger types, one returns a literal `False` with a "not implemented fully" comment, `frequency` is `pass`, `composite` was never written; actions are log/noop (`log_event` → pass, `notify` → a log line); net: a 126-line worker + 2 tables + a beat entry delivering ~zero user value. Its *ideas* are all better homed: **contradiction detection** → already exists for real in A6's reconciler + A8's negated edges (D1's agent consumes those directly); **staleness watching** (the `absence` rule on pending_items) → C7's session-start maintenance / E3's Stale Work Detector; **`schedule_worker`** → C7's trigger machinery. **Plan (removal is with D1, not now):** when building D1, port the pending-edge-pileup query and slot-staleness check as *agent heuristics*, then delete `sentinel_monitor.py`, the `Sentinel*` tables, and the beat entry. Until D1, the Sentinel keeps running harmlessly. Completing it standalone would be sunk-cost investment in a parallel notification system nobody reads. **→ spec: folded into [docs/specs/D1_D2_maintenance_agent.md](specs/D1_D2_maintenance_agent.md).**
- [ ] **D3 Agentic telemetry** `(new)` — Expose the agent's decisions/reasoning through SSE so its actions are visible (feeds Track F telemetry). **S1 note (2026-07-10): no spec needed** — discharged by D1's named structlog events (`agent_action`, `agent_run_summary` — **live since D1, 2026-07-17**) + E7's `mcp_tool_call`; the remaining work is F5's SSE promotion (see the D1 spec §6 and F_design_brief).

## Track E — Coding ICE / Project State Engine

> **North star (settled in-session 2026-07): kill the persistence tax.** What makes an AI agent productive on a long-lived project today is a *human* who hand-maintains a CLAUDE.md, an architecture doc, and a roadmap — with discipline almost nobody has time for. Coding ICE exists to BE that discipline automatically: the persistent, always-current **context authority** for a codebase, consulted **inside the agent's loop at every step** (not just session start), so the model stops wasting turns re-exploring, reading the wrong files, and re-deriving old decisions. (Evidence this matters even with good scaffolding: mid-session, with CLAUDE.md pointing at the roadmap, the assistant still read VISION.md + rough notes instead — in-loop steering can't rely on model discipline; the system must serve the right pointer at the right moment.)
>
> **Architecture decisions (settled 2026-07 — these supersede older framings in the entries below wherever they conflict):**
> 1. **Brain, not body — MCP-first, no own harness.** ICE does NOT become an agentic coding harness (that's months of rebuilding Claude Code badly before the memory features — the actual point — even start). ICE is a **local MCP memory/context server** that any existing harness (Claude Code, Cursor, Cline, …) mounts. The harness does file I/O, editing, looping, tests; ICE answers *what matters, where it is, why it's that way*. **E5 is thereby RESOLVED** — no Aider adapter, no own executor. A *thin dispatcher* that hard-forces "consult ICE before each step" is a recorded contingency ONLY if pull-discipline (tool descriptions + a skill/system-prompt rule) proves insufficient in real use.
> 2. **"Where, not what."** ICE serves **pointers + rationale** — `symbol → file:line`, edges, decisions, typed notes — never code bodies. The harness's own tools read the actual lines. Pointers survive edits; injected source goes stale instantly and burns budget. The code graph stores structure, never source text.
> 3. **One memory substrate; coding is an overlay.** Conversational memory (episodic/codex/procedural/slots) is not "the other mode" — it is the **rationale layer** of coding memory: the *why* lives in conversations about the code and is never in the code itself. Coding mode = conversational base + structural overlay (code graph) + code-aware serving (pointers) + git-as-timeline + the MCP tool surface. There is no conversational/coding boundary to police — that boundary was the convolution.
> 4. **Four-layer memory model for a codebase:** **Structural** (files/symbols/imports/calls — deterministic static analysis — served as pointers) · **Rationale** (decisions, rejected alternatives, the why — extracted from conversations + commits — served as typed notes) · **Temporal** (git IS the timeline; read it, don't re-store it) · **Behavioral** (conventions/patterns — procedural extraction — served as rules).
> 5. **Work-unit trigger.** "A unit of work finished → reconcile memory" is ONE concept with two signals: **git commit / task-done** in coding (semantic and precise — never guess with timers when a commit exists) and **session-gap** in conversation (C6/C7). E3/E4 fire on the former; C7's catch-up fires on the latter.
> 6. **Build vs external:** **project-specific knowledge → ICE owns it** (deterministic extractors, cheap); **world-generic knowledge → external tools fetch it** (harness WebFetch/search, doc MCPs) and ICE stores only the pointer + the project-relevant summary. We do NOT build a library-docs indexer.
> 7. **Prerequisite — the service layer (E0).** Every user-facing capability becomes an interface-agnostic service with thin adapters: REST (frontend, F1), MCP tools (E7), chat-commands (C11), workers internally. Nothing new gets built frontend-coupled again.

- [x] **E0 Service layer extraction** `(new — foundational; gates E7, F1, C11)` — **Done 2026-07-17 (commit `f43feeb`, with E7; spec rev notes 1–10 recorded before coding per rule 12).** Shipped `src/services/` — `errors.py` (NotFound/Validation/Conflict) + `bookmarks.py` (bookmark/list/latest-turn + `remember_note`, ice_remember's note store) + `slots.py` (**VALID_SLOTS moved here** — the C9 widening seam; CRUD + `append_to_slot`) + `scoping.py` (G16 privacy re-sync verbatim + label overrides) + `clusters.py` + `review.py` + `registry_svc.py` (fcntl sidecar lock around load-modify-save) + `graph.py` (wraps T4's evolution functions AS BUILT; `entity_edit` is where manual description editing is BORN — journals `log_description_update(source="mcp_edit"|"manual_edit")`, regenerates `context_payload`, never writes it) + `retrieval_svc.py` (`context_for` = classify → B2 → orchestrate over the live classifier via the new lazy `ICECore.classifier`, G13; returns STRUCTURED fragments; + `recent_turns`/`conventions`/`session_start_block`). Plain `fn(db, ...) -> dict`, zero FastAPI imports (grep-gated in the test). Routers rewritten as thin adapters (`routers/adapter.py::service_errors()`); **byte-identical REST surface proved by the record-and-compare harness [tests/test_router_parity.py](../tests/test_router_parity.py) — 31/31** (baseline in gitignored `logs/`, contains live-DB content). The ONE remaining intentional change (D2b — D2a's bookmark enqueue had already shipped with C7): review-approve **applies** and carries the D1/D2 arms — `entity_merge` → `src/workers/codex_ops.py::merge_entities` (**loud NotImplementedError stub; D1 builds the real merge**), `codex_reconciliation` → apply-the-supersession via `_expire_edge` (journaled); `reject()` added (service only, no new REST endpoint — F1's job). Validated 48/48 by [tests/test_services.py](../tests/test_services.py) (round-trips, both arms, registry-under-lock, graph journaling + payload regeneration, G16 both directions, lease boot logic, grep-gate). **Look-ahead honored:** C11 = adapter #3 over these exact functions; F1/F2 build REST against them; D1's agent gets review/graph services router-free; G24's to-thread question dissolved (routers stayed sync-def — FastAPI threadpools them).
- [x] **E1 Project State Engine schema** `(new — reworked 2026-07)` — **Done 2026-07-18 (commit `fe8df63`, migration `c4e9b7d15a20`; spec rev notes 1–13 recorded before coding per rule 12).** Shipped `projects` (name/slug/`roots TEXT[]`/settings) + `project_state` (goal, branch, last task/session, `last_reconciled_commit`) + bi-temporal `decisions` (valid_from/valid_until/superseded_by, embedding, `decision_type ∈ decision|constraint|incident`) + `tasks` (commit_hashes, files_changed, updated_at-driven staleness); `daily_checklist` is a **VIEW** over tasks as amended; `architecture_clusters`/`development_patterns` deliberately never created (A7.4 communities / `procedural_memory.project_id` serve them). Conversations attach via `conversations.project_id` (`scoping.set_scope(project=…)`; incognito refuses attachment). CRUD in `src/services/projects.py` (E0 pattern), registration via `scripts/register_project.py` or `ice_control project_register`. Validated in `tests/test_coding_core.py` (checks 1, 6, 9–11). **Look-ahead:** C9's project slot tier hangs off these exact tables; A7.4 runs over the code graph; G17 provenance via `decisions.source_batch`. *(Original intent below for history.)* Add a `projects` first-class entity (was missing): a project anchors N conversations + a codebase root + its docs + its knowledge units (E9) + per-project slots (C9) + scope; conversations attach to a project, and `memory_scope_type=="coding"` implies one. This is what "conversational memory per project" hangs off — A5/C6's batch-set scoping resolves a project to its conversations' batches, so the retrieval seam already fits. Tables largely as drafted — `project_state` (goal, branch, last task/session), `architecture_clusters`, `decisions`, `tasks` (commit hashes + files_changed), `development_patterns` — with two changes: `decisions` gets temporal versioning + supersession **reusing A6's reconciler patterns** (a new decision supersedes/conflicts like codex edges do), and **`daily_checklist` is dropped as a table** — it's a *view* derived from tasks + staleness + project_state, not a store. Design against A7 (one codex) and E6 (OKF units). **→ spec: [docs/specs/E_coding_core.md](specs/E_coding_core.md) (S1 2026-07-10 — covers E1/E1b/E8/E9/E10, folds E2/E3/E4/E6; drops `architecture_clusters` + `development_patterns` tables with reasons.)**
- [x] **E1b Code-structure graph (moved from A7)** `(new — amended 2026-07)` — **Done 2026-07-18 (commit `fe8df63`).** Shipped [src/coding/code_graph.py](../src/coding/code_graph.py): Python-first stdlib-`ast` `CodeExtractor` (language seam open for tree-sitter) → typed entities (`module`/`class`/`function`, canonical `"{slug}:{module}.{qualname}"` casefolded, display name in properties) with **pointers only** (file_path, line range, signature, docstring FIRST line), `source='static_analysis'`, `project_id`, empty aliases + NULL embeddings (conversational extraction can never re-attach; the agent's cosine channel skips them). `imports` + best-effort `calls` edges (same-module / imported names / `self.` / dotted-module attrs), **resolved 1.0 vs heuristic 0.6 via `extraction_confidence`** (codex_edges has no properties column — spec rev 3), all under the deterministic per-project batch `uuid5("ice:code-graph:<id>")`. Derived-memory rules live: decay-exempt (`codex_decay` gained `source='conversation'` on all three UPDATEs), **zero CodexEvents**, excluded from conversational retrieval via `_entity_source_filters()`/`_entity_visible()` + `_codex_scope_sets`' code-graph allowance (project-scoped queries get the union sets, never unscoped fallback). Incremental delete-and-recreate per changed file with canonical-stable ids (uuid5) so cross-file inbound edges survive; parse errors keep the last good map + a `parse_error` marker; 20k-file cap; heuristic ratio logged at bootstrap (empirical deferral: >60% ⇒ imports-only). Validated: checks 1–5 in `tests/test_coding_core.py` (mini_repo fixture: 17 entities, 6 edges, 1 heuristic; ICE-itself bootstrap in the registration report). *(Original intent below for history.)* The code-aware half of Codex V3: deterministic static-analysis extractor (AST parse → functions/classes/modules/files as **typed** codex entities via A7's `entity_type`, with deterministic `imports`/`calls`/`inherits`/`defined_in`/`tested_by` edges tagged static-analysis-sourced), real import/call resolution, not regex. **Amendments:** (a) **Pointers, not bodies** — entities store `file_path`, line range, signature, docstring-summary; never source text (decision 2). (b) **"One codex, namespaced" — supersedes the flat "one codex" constraint and answers the "do we need 2 codexes?" question:** same `codex_entities`/`codex_edges` tables, but code entities carry `project_id` + source tag and are **derived memory** — regenerable from source, excluded from conversational-mode retrieval unless the conversation is attached to that project, **exempt from `codex_decay`** (staleness is determined by reconcile-on-commit against git, not by time), safe to bulk-rebuild. Conversational entities remain **earned memory** (irreplaceable, decaying, corroborated). Cross-links between the spaces (decision ↔ function, discussion ↔ module) are the whole payoff of one graph — keep them first-class. Scale rationale: a medium repo yields 10³–10⁴ derived entities vs ~10³ conversational; the namespace partition is what keeps retrieval clean at that ratio. Reuses A7's community layer over the code graph (A7.4 — dense import/call structure is where communities finally pay). **Edge cases from agentic-coding experience (2026-07):** (a) **mid-session staleness** — the graph is *commit-fresh, not keystroke-fresh* (E3 reconciles on commit); while the agent edits, line numbers drift — so pointers are **symbol-first** (a name is grep-able and survives edits; the line number is a hint carrying as-of metadata, E7), never treated as ground truth mid-session; (b) **branches** — the graph tracks the working tree's HEAD; a branch switch triggers an incremental re-sync of files that differ (E3), so the map matches what the agent actually sees on disk; (c) **monorepos/multi-root** — `projects` should tolerate multiple roots or nested sub-projects later; don't hard-wire one-root-one-project into the schema. **→ spec: [docs/specs/E_coding_core.md](specs/E_coding_core.md) (multi-root `roots TEXT[]` from day one; Python-first `ast` extractor; derived-memory rules incl. no-journal + decay-exempt.)**
- [x] **E2 Coding-mode routing** `(new)` — **Done 2026-07-18 (commit `fe8df63`, folded into E-core per spec D11).** A project-attached conversation resolves its scope to the project's conversations (`scope["conversation_ids"]` — the episodic legs' new list-capable `_conv_scope_filter`, C6's seam arrived early) ∪ the code-graph allowance (D3 filter); B2 gained `coding_scope` → `ltm_bump_coding` (+0.7, breakdown-logged, bump-not-force). Coding intent labels wait for B1's single bundled retrain, as recorded there. Validated: check 4 + the D11 checks in `tests/test_coding_core.py`. *(Original intent for history:)* Branch in `chat_completions` on the coding scope; coding-specific intent labels for the classifier (ties to B1 — retrain once, together).
- [x] **E3 State-reconciliation agents** `(new — amended 2026-07)` — **Done 2026-07-18 (commit `fe8df63`).** Shipped [src/coding/reconciler.py](../src/coding/reconciler.py): **hook-first** (consent-installed post-commit hook → `POST /user-control/projects/{id}/commit` → the `"commit"` work-unit handler registered in `create_core()` → `project_reconcile` cpu-lane event job; hook falls back to a `$GIT_DIR/ice_pending_commit` marker when the app is down), polling strictly the fallback (`project_poll` job, 600s — marker files + HEAD drift). Per commit-range: incremental E1b re-parse of changed `.py` files → hash-gated E9 re-derive → commit/file linking to the active task → cue-gated decision extraction over commit messages (enqueued to the gpu-lane `decision_extract` job — the reconcile job itself never touches the LLM) → state advance. Force-push/unknown-base → full re-parse; unreachable root → flagged in `project.settings` (tools say so, never silently stale). **Stale-work went to the agent worklist as decided:** `stale_work` DETECTOR in `maintenance_agent.py` (tasks untouched >14d → Tier-2 review proposal, no LLM, pending/rejected-blocked — spec rev 1); the reconciler writes no worklist items itself. Dependency-tracker/pattern-extractor intents live inside the graph (import edges) and procedural extraction (project-tagged) — no separate agents. Validated: checks 2, 9, 11 in `tests/test_coding_core.py` + `test_maintenance_agent.py` 43/43. **Known gap (2026-07-18, → E11):** both triggers are `HEAD`-anchored, so uncommitted in-session work never reconciles — closed by **E11 reconcile-on-read** (pull-based working-tree freshness at query time; the machinery here — working-tree parse + `sync_files` — is exactly what E11 reuses). *(Original intent below for history.)* Coding counterparts of the workers: State Reconciler (git-diff scan → clusters/decisions/tasks), Dependency Tracker (import graph, circular-dep flags), Stale Work Detector, Pattern Extractor. **Amendments:** **commit-triggered** (the work-unit signal, decision 5 — not timer-based), **incremental** (re-analyze only files in the diff), and they maintain the *derived* layers (structural graph E1b, project knowledge units E9, staleness flags). The rationale layer is maintained by E8's decision extractor, not these. **→ folded into [docs/specs/E_coding_core.md](specs/E_coding_core.md) D5 (consent-installed post-commit hook + idle polling fallback; stale work feeds the D1 agent worklist).**
- [x] **E4 Session-start checklist flow** `(new — amended 2026-07)` — **Done 2026-07-18 (commit `fe8df63`).** Shipped in `src/services/projects.py`: `session_start_data`/`render_session_start` — project_state (goal/branch/last task) + `git diff --stat` since `last_session_at` (reflog `HEAD@{date}`, error-tolerant) + open constraints + ≤3 pending/stale tasks + ≤3 freshest decisions. **One renderer, two adapters:** the chat assembler prepends it (`=== PROJECT SESSION START ===`) only when the turn OPENS a sitting (preflight gap check — spec rev 8; `last_session_at` advances once per sitting), and E7's `ice://session-start` resource appends per-project blocks under the slots+summary body (URI stable, enriched in place as promised). Registration bootstrap = full E1b parse + E9 derive with a printed report; **git-log replay accepted-and-recorded but off by default** (honored when F10's importer lands); docs ingestion still waits on C12 (E10). Validated: check 10 in `tests/test_coding_core.py` + `test_mcp_server.py` 21/21. *(Original intent below for history.)* On coding-session start: restore project_state, `git diff --stat` since last session, surface pending/stale decisions+tasks, assemble the "welcome back" context block (also exposed as E7's session-start MCP resource). **Plus project cold-start bootstrap** on registration: full E1b parse of the repo, ingest README/docs (E10 → C12 machinery), extract deps/schema/config (E9), and optionally replay the git log as temporal memory — F10's "system lives through it" philosophy applied to a repo's history. **→ folded into [docs/specs/E_coding_core.md](specs/E_coding_core.md) D6 (git-log replay optional + off by default; docs ingestion waits on C12's backend).**
- [ ] **E5 Executor decision: own harness vs Aider** `(RESOLVED 2026-07 — see decision 1)` — **MCP-first; no own harness, no Aider adapter.** ICE is the brain under an existing harness; building an executor would spend months rebuilding commodity harness robustness before any memory feature lands. Entry kept for history; the recorded contingency is a *thin dispatcher* forcing ICE-first consultation per step, only if pull-discipline empirically fails. *(Original framing: adapter `plan(prompt, files) → PlanResult` / `execute(plan, files) → DiffResult`; Plan/Act split — now the harness's job.)* **Frontend-hosted coding (user question, 2026-07 — answered):** "can I code from ICE's own frontend?" — the **primary path is the reverse** (ICE mounted as MCP inside the harness you already use); a coding session *from our frontend* is a **recorded later option**, done by embedding a headless harness (Aider's API mode is the lightest candidate) behind the same adapter seam, with ICE exposing it the same MCP tool surface (E7) and E4's session-start context. Not built until the MCP path has proven the memory layer; if built, it's a UI + adapter shim, not a new engine (see the Track-F ledger line).
- [x] **E6 OKF-inspired knowledge units** `(new)` — **Done 2026-07-18 (commit `fe8df63`, settled inside E9 per spec D9).** OKF units ARE derived codex entities: `entity_type='project_fact'`, `source='derived'`, structured `properties` (kind/data/source_files/content_hash) + human `description` + pointer — no separate machinery, no markdown files. *(Original intent for history:)* Adapt OKF's typed-knowledge-document philosophy (type/title/description/resource/tags) *into our tables/Codex*, not as markdown files.
- [x] **E7 ICE-MCP server** `(new — the coding interface)` — **Done 2026-07-17 (commit `f43feeb`, with E0).** Shipped [src/mcp/server.py](../src/mcp/server.py) on the official `mcp` SDK (FastMCP), stdio default + `--http`, installed as the **`ice-mcp`** entry point (pyproject `[project.scripts]` + hatchling build-system — uv installs no entry points for build-system-less projects, spec rev 2). **Exactly 10 tools** — composite: `ice_context` / `ice_why` (note + T4 timeline) / `ice_recent` / `ice_conventions` / `ice_where` (**E1b-gated**: codex name/alias resolution, honest description; engine swaps under a stable signature when the code graph lands) / `ice_remember(text, slot|bookmark)`; micro (action-multiplexed, unknown action → error listing the enum): `ice_slots(list|get|set)` / `ice_graph(view|edit|edges|timeline|diff)` / `ice_control(scope_get|scope_set|review_list|review_approve|review_reject|registry_view|registry_edit)` (`decisions_*` land with E1 — NOT pre-stubbed) / `ice_bookmarks(list|add)`. Every handler a short hop into an E0 service; **every call journals `mcp_tool_call`** (the E5 pull-discipline telemetry — the dispatcher stays unbuilt behind that measured deferral); writes apply immediately + journal with `source="mcp_edit"` (D8/G17). `ice://session-start` resource renders slots + last-session summary (E4 enriches in place). **D6 boot as decided:** attach-if-running else `docker compose up -d postgres` (clear stderr + nonzero exit if docker's absent); `create_core()` lease-checked — **the lease was made concrete as a `runtime_lease` ledger pseudo-row** stamped by the owning tick, and **defer = STANDBY runtime** (spec rev 3/10: event jobs for the process still run — a literal no-runtime would silently drop post-flight/bookmark extraction; periodic dispatch + burst + stamping stay with the owner; standby self-promotes when the owner's lease goes stale — same check both directions, so the app defers to a lingering MCP session too); **linger** on shutdown (docker stays up, `ice_core_linger` logged; `./stop_ice` remains the full-stop path). stdio detail: structlog rerouted to stderr in `main()` (stdout is protocol framing). Validated 21/21 by [tests/test_mcp_server.py](../tests/test_mcp_server.py) (in-process FastMCP client: schemas, round-trips, mcp_edit journaling, seeded-DB `ice_context` fragments, resource render) + a real stdio boot listing all 10 tools; lease/standby/promotion boot logic in test_services.py. **Look-ahead honored:** tool names/signatures stable for E1/E1b/E8's engine swaps; session-start body is E4's to replace; `mcp_edit`/`manual_edit` vocabulary matches D1's `maintenance_agent` (G17). **USER-REQUIRED: DEFERRED BY USER DECISION (2026-07-18) — do NOT remind.** Registration (`claude mcp add ice -- uv run --directory /home/deepnar/Programs/ice ice-mcp`) is intentionally postponed until ICE's memory stores hold enough to be worth querying (mounting an empty memory has no value). The user reaffirmed this after ~2 earlier sessions kept surfacing it as "open." Revisit ONLY when the user raises it. *(Original intent below for history.)* The MCP surface over E0's services + the memory stores; this is how any harness mounts ICE. **Read tools** (the in-loop context authority): `ice_context(task)` — "what matters for this task" as pointers + decisions + patterns; `ice_where(symbol)`; `ice_why(entity|decision)`; `ice_deps(file|module)`; `ice_recent(project)` (temporal layer); `ice_conventions(project)` (behavioral layer). **Write tools** (the full user-control surface, so chat can do everything the frontend can — bookmarks, slot writes (project-scoped), decision records, scope toggles, review-queue resolution). Session-start resource = E4's welcome-back block. Every tool call emits F5 telemetry. Pull-discipline (getting the harness to actually call `ice_context` before grepping) is handled via tool descriptions + a skill; if that empirically fails, the E5 contingency dispatcher exists. **Amendments (user questions, 2026-07):**
  - Two-tier tool surface (6 composite + 4 action-multiplexed micro), chat-as-correction-UI (apply + journal — user-decided), the headless boot story (attach-else-boot with linger + lease — user-decided), and staleness honesty (as-of metadata, symbol-first pointers) are ALL settled in the spec.
  - **→ spec: [docs/specs/E0_E7_services_mcp.md](specs/E0_E7_services_mcp.md) (S1 2026-07-10 — user decisions settled: boot = attach-if-running else boot-headless-core with linger, runtime singleton guarded by C7's lease; chat writes apply immediately + journal. Surface = 6 composite + 4 action-multiplexed micro tools; E5 dispatcher stays unbuilt behind a measured pull-discipline deferral.)**
- [x] **E8 Decision extractor + auto-architecture-doc** `(new — the rationale layer; the persistence-tax killer)` — **Done 2026-07-18 (commit `fe8df63`).** (a) Shipped [src/workers/decision_extractor.py](../src/workers/decision_extractor.py): **cue-gated** (constraint > incident > decision cue families; code blocks stripped; **no cue ⇒ no LLM call, verified** — A6 stance) bounded JSON extraction (temp 0, via the agent's `make_llm_decider` contract) of `{decision, rationale, alternatives_rejected, files_affected, type}` into E1's bi-temporal table. Dedupe/supersession by embedding similarity against the project's active decisions (settings thresholds): ≥0.95 + same type/files ⇒ silent duplicate; ≥0.85 + overlapping files ⇒ **auto-supersede** (valid_until + superseded_by — Tier 1); ≥0.85, disjoint files ⇒ new row + `decision_supersession` review proposal (Tier 2). Runs as a direct call in the post-flight gpu chain for project-attached turns AND over commit messages at reconcile (gpu-lane `decision_extract` job). **Both added types live:** `constraint` rows surface **FIRST** in `ice_context` whenever the task mentions their files (`retrieval_svc.constraints_for_task`, path-token matching, score 10.0); `incident` rows require the error→fixed arc. `decision_cue_fired` logged (empirical deferral: widen cues if real decisions slip). (b) **Arch doc as a render:** `services/graph.py::render_architecture_doc` — module tree w/ docstring one-liners + key decisions w/ rationale + constraints + conventions (project procedural rows) + last-15 git log read LIVE; served via `ice_control action=arch_doc`. Validated: checks 6, 7, 12 in `tests/test_coding_core.py`. *(Original intent below for history.)* (a) **Decision-extractor worker** (sibling of codex_extractor; runs over coding-session turns + commit messages): captures `{decision, rationale, alternatives_rejected, files_affected, timestamp}` into E1's `decisions` table **at the moment the why is expressed** — ICE can only store a why that was actually said, so capturing it live during work is what replaces the human writing docs afterwards. (b) **Architecture doc as a view, not a file:** assembled on demand from the unified graph — structural skeleton (E1b) + rationale (decisions + A7 rich notes) + temporal (git) + behavioral (patterns) — rendered as markdown for humans, served as pointers for the loop (E7). Same knowledge, two renders; nothing to hand-maintain, nothing to go stale. This is the direct answer to "automatically make the AI produce an architecture doc like mine, with every what/why/how and decision." **Two decision *types* added from agentic-coding experience (2026-07) — the knowledge agents most painfully lack:** (a) **constraints / do-not-touch** — "we deliberately do X, don't refactor it", "Y is fragile, change only with Z" — stored as first-class decision records (polarity kin of A8's negations) and surfaced *prominently* by `ice_context` before an agent touches the affected files; (b) **incident/fix memory** — recurring errors and their resolutions `{problem, root cause, fix, files, date}` captured by the same extractor when a debugging session resolves — so no agent re-debugs the same environment quirk twice (the "tests need docker up" class of knowledge). **→ spec: [docs/specs/E_coding_core.md](specs/E_coding_core.md) D7/D8 (cue-gated bounded extraction; decisions table bi-temporal; arch-doc = `render_architecture_doc` service).**
- [x] **E9 Project knowledge extractors** `(new — the "small things we miss")` — **Done 2026-07-18 (commit `fe8df63`).** Shipped [src/coding/project_facts.py](../src/coding/project_facts.py): six deterministic parsers (no LLM) — deps+versions (pyproject/package.json, uv.lock resolution), DB schema (`__tablename__` ast scan + alembic head from the revision graph), commands (Makefile targets/npm scripts/pyproject entry points), config surface (BaseSettings fields + `.env`/`.env.example` **KEY NAMES, never values — verified**), infrastructure (compose services/images/ports, no yaml dep), data shapes (`*schema*.json`, size-capped). Each → ONE `codex_entities` row (`entity_type='project_fact'`, `source='derived'`, uuid5 id, structured properties + human description + file pointers), **hash-gated re-derive** (content_hash over source files; reconcile passes `changed_files` so untouched parsers are skipped entirely). Validated: check 8 in `tests/test_coding_core.py` (all six fire on the fixture; pyproject edit re-derives deps while infrastructure's hash stands). *(Original intent below for history.)* Deterministic parsers for project facts *outside* the code graph: **dependencies + versions** (pyproject/package.json/lockfiles), **DB schema** (ORM models + migrations → a current-schema unit, re-derived on migration change), **run/test/build commands** (scripts, Makefile/justfile, CI configs), **config surface** (settings classes + .env key names — never secret values), **infrastructure** (docker-compose services/ports/images), **data shapes** (schema/fixture files the project itself treats as structure — e.g. a label_schema.json). Stored as E6 OKF-typed units, **pointer-first** (summary + file pointer, not a copy), reconciled by E3 on change. Per decision 6 these are project-specific → we build them; they're cheap deterministic parses, not LLM work. **→ spec: [docs/specs/E_coding_core.md](specs/E_coding_core.md) D9.**
- [ ] **E10 Docs-for-coding** `(new)` — *(Update 2026-07-18: everything E10 needed from the E-core exists — `projects` scope, E9's dependency list, C12's entry already routes project docs through the pipeline "under the project's scope". E10 now waits ONLY on C12's backend; nothing else blocks it.)* Split by decision 6. **Project-internal docs** (README, docs/, design notes, dropped PDFs/specs): ingested through **C12's pipeline** under the project's scope — chunked, codex-extracted, clustered; real ICE memory. **External library documentation: NOT indexed by ICE** — ICE knows *which* libraries + versions the project uses (E9) and routes doc lookups to external tools (harness WebFetch / doc MCPs), storing at most a pointer + the project-relevant takeaway as an E6 unit. No PyPI-scale doc scraper, ever. **→ spec: [docs/specs/E_coding_core.md](specs/E_coding_core.md) D10.**
- [x] **E11 Reconcile-on-read (working-tree freshness)** `(new — 2026-07-18, user-driven; THIS ENTRY IS THE SPEC, no separate file)` — **Done 2026-07-19 (impl `548b433`).** Shipped as specced: `reconciler.freshen_working_tree` (porcelain gate → ignore-filtered dirty-`.py` set, renames both sides → `(path,mtime,size)`-signature + 2.0 s min-interval throttle → the existing `sync_files`; never touches `last_reconciled_commit`); the D4 advisory lock went INSIDE `sync_files` — the one choke point all writer paths share (read-freshen, commit reconcile, bootstrap `full_sync`), so both specced paths are guarded at one site; service-layer gates in `where_symbol` (+ optional `project` param, D3 order) / `context_for` (project scope) / `render_architecture_doc`; `ice_where` passes `project` through untouched (E0 rule). **One re-grounding refinement (rule 12):** D3's "`ice_context` already has `scope["project_id"]` — automatic" was false at HEAD — `context_for` never derived project scope from the conversation row, so the gate would have been unreachable from `ice_context`; fixed by mirroring main.py's D11 enrichment (conversation → `project_id` + project `conversation_ids`) inside `context_for`. New settings `reconcile_on_read` / `reconcile_on_read_min_interval_seconds`; no migration. Validated: `tests/test_reconcile_on_read.py` 16/16 (uncommitted edit visible through `where_symbol` AND `context_for`, anchor untouched, signature/interval throttle, clean-tree fast path, kill switch, D3 resolution incl. unknown-project tolerance, a real two-session advisory-lock blocking test); regressions coding-core 46/46, services 48/48, mcp 21/21, smoke 69. Look-ahead intact: `freshen_working_tree` is the seam for an optional debounced watchdog watcher if Z1 shows pull latency. *(Original spec below for the record.)* **The gap:** E3 makes the code graph *commit-fresh, not keystroke-fresh* — both triggers (the post-commit hook and the `project_poll` job) are anchored to `HEAD` moving. For a user doing large in-session work **without committing** (the maintainer's actual workflow), `HEAD` never moves, so neither trigger fires and the graph silently drifts from the working tree (new symbols missing, deleted ones lingering, moved code mis-pointed). Symbol-first pointers only cover *line* drift, not *structural* drift. Close it with a **pull-based** freshener (chosen over an always-on file watcher: zero idle cost, and the graph is guaranteed correct *at the moment it's queried* — the only moment it matters). **Decisions (all settled):**
  - **D1: gate lives in the service layer, never the adapter.** `ice_where`/`ice_context`/`arch_doc` are MCP tool calls in agentic mode (also REST + future chat) — all thin adapters over `src/services/`. The freshness call goes in `graph.where_symbol`, `retrieval_svc.context_for` (only when scope carries a `project_id`), and `graph.render_architecture_doc`, so every surface inherits it. **NOT** triggered by episodic reads (`ice_recent`, `conventions`).
  - **D2: new service fn `reconciler.freshen_working_tree(db, project) -> dict`.** (a) cheap gate: `git status --porcelain` in the project root → the ignore-filtered set of changed/added/deleted/untracked `.py` files; empty ⇒ fast-return (the clean/just-committed case costs one `git status`). (b) throttle: skip if a signature = hash of sorted `(path, mtime, size)` over the dirty set is unchanged since the last freshen, OR the last freshen was < `reconcile_on_read_min_interval_seconds` (2.0) ago — so a burst of reads with no edits is free. (c) reparse: call the EXISTING `extractor.sync_files(db, dirty_py_files)` (incremental delete-and-recreate; `parse_file` already reads the working tree; deleted/untracked handled by git-status status codes). (d) **must NOT advance `last_reconciled_commit`** — sync_files already doesn't touch `project_state`; keep it that way (the commit anchor stays a commit concept; on the next real commit the diff-based `reconcile_project` re-runs over the same files, idempotent).
  - **D3: `ice_where` needs to know WHICH project.** Resolution order: explicit `project` arg (add optional param to `ice_where` + `where_symbol`) → the `slug:` prefix if the queried symbol carries one → the sole registered project if exactly one exists → else freshen nothing (bare cross-project lookup stays commit-fresh, documented). `ice_context` already has `scope["project_id"]` — automatic.
  - **D4: concurrency.** A read-freshen and a commit-`reconcile_project` can race (agent queries while the hook fires) — both do per-file delete-and-recreate. Guard *both* paths with a Postgres advisory lock keyed on the project id (`pg_advisory_xact_lock(hashtext(:pid))`) so at most one reparse of a project runs at a time; the loser either waits briefly or skips (its work is redundant). Cross-process safe (app + ice-mcp).
  - **D5: latency + config.** Parse all dirty files (a laptop reparses ~50 files sub-second); log a warning if the dirty set is large (>50 — a Z1 signal) but don't cap. New settings: `reconcile_on_read: bool = True` (kill switch), `reconcile_on_read_min_interval_seconds: float = 2.0`.
  - **Validation** (extend `tests/test_coding_core.py` or a sibling): edit a fixture file **without committing** → `where_symbol`/`context_for` reflect the new/removed symbol; `last_reconciled_commit` did NOT advance; a second read with no further edits does zero reparse (throttle/signature); the clean-tree fast path runs only `git status`; kill-switch off ⇒ commit-fresh behavior. **Traps:** don't advance the commit anchor; don't reparse on episodic reads; don't put the gate in the MCP handler (the other adapters would stay stale); don't drop the throttle (every keystroke-triggered agent read would re-`git status`). **Look-ahead:** if pull-latency ever bites (measured at Z1), the same `freshen_working_tree` becomes the callback for an optional debounced `watchdog` file watcher (push) — the function is the seam, so that upgrade is additive.

## Track F — Frontend & UX (custom frontend era)

> **⚠ DESIGN FIRST — ask the user.** The whole frontend is a large, opinion-heavy consolidation. Before building any of F, hold a dedicated design conversation with the user (layout, stack, which panels, the graph view, telemetry, how modes/scoping surface). Do NOT build straight from these entries — they are intent, not a spec.
>
> **End state — the scripts go away.** `./ice`, `./stop_ice`, and `setup.sh` are development scaffolding. In the final product they are **replaced** by a single combined frontend+backend application: one installer sets everything up, and opening the app boots the whole stack automatically — no manual scripts, no terminal. *(C7 made this dramatically smaller, 2026-07-11: the stack is now docker/postgres + the proxy process, which owns its own maintenance in-process via `create_core()` — no redis, no celery worker+beat, no default vLLM side-server. The packaged app boots postgres + one process; `create_core()` is also E7's headless entrypoint.)* Plan the frontend so this packaging is the target.
>
> **End-state addendum (user, 2026-07) — the MCP path boots a SEPARATE way. DECIDED AND SHIPPED with E7 (2026-07-17).** The packaged-app story above covers the *frontend user*: open the app → stack boots. ICE-as-MCP runs under an external harness with no ICE frontend open, and its headless boot is now: **attach-if-running else boot-headless-core, with linger** — `ice-mcp` connects to postgres (or `docker compose up -d postgres` if down), calls the same `create_core()` the app lifespan uses, and the **runtime-lease/standby arbitration** (ledger `runtime_lease` row) guarantees at most one maintenance dispatcher across the app + any number of MCP sessions (deferred cores run event jobs in standby and self-promote when the owner exits). On MCP shutdown the core stops but docker lingers; `./stop_ice`/the packaged app remain the explicit all-the-way-down paths. The packaged-app work (F-track) inherits this: "the frontend app is what boots ICE" is one path of two, and both go through `create_core()`.
>
> **UI surfaces OWED to other tracks (2026-07 — the cross-track ledger, so nothing lives only in anyone's head).** These are specced in their home items; the frontend design conversation must cover every one of them, and F1's foundation must leave room for them:
> - **C6:** sidebar **scope toggles** (tick extra conversations into scope for cross-chat retrieval) + **`@`-mention** of a specific conversation/turn for one-off context + the none-scope "incognito" indicator (G16).
> - **C9:** slot editing at all **three tiers** (global / project / conversation).
> - **C11:** the chat-command affordance — **the parser SHIPPED 2026-07-19** (`src/api/chat_commands.py`, v1 set /remember /slots /bookmark /search /scope /forget /delete-conversation /help, `/help` self-documents); F owes only **discoverability** (autocomplete/hints/command palette), and the `chat_command` telemetry counts feed that design conversation.
> - **C12:** per-conversation **document upload** + sidebar "add existing doc to this conversation's scope".
> - **E1/E2:** **projects UI** — create/register a project (point at a codebase root), attach conversations to it, the **Conversation ↔ Coding mode toggle**.
> - **E4:** render the **"welcome back" session-start block** for coding sessions.
> - **E8:** a **"view architecture doc"** surface (rendered markdown view over the graph).
> - **G23:** **export / backup** buttons (memory is the user's — give them the door). *(Backend SHIPPED 2026-07-19: the buttons wrap `scripts/ice_export.py` / `scripts/ice_backup.sh` — no new logic.)*
> - **E5 (conditional):** if frontend-hosted coding is ever pursued (embedded headless harness — see E5's note), it needs a coding-session UI (plan review, diff view, test output); not owed until that option is exercised.
> - Already first-class F items: review queue (F2), graph view (F3), settings (F4), telemetry (F5), feedback (F9), import (F10), replay/branching (F13 — also B6's UI), hardware advisor (F15).

- [ ] **F1 Custom web frontend foundation** `(planned — F5 in the old missing-items table)` — The big one; all user-control APIs already exist, the frontend surfaces them. Everything below rides on it. **Updated 2026-07:** the frontend is the REST *adapter* over E0's service layer (peer of E7's MCP tools and C11's chat-commands) — build it against the services, not against bespoke endpoints, so features stay interface-agnostic.
- [ ] **F2 Review-queue panel** `(bug-adjacent)` — Reflection's slot/cluster proposals currently rot in `review_queue` forever because nothing renders it. Highest-value early frontend piece. **Item inventory as of C10/C11 (2026-07-19):** types now include `memory_slot_update`, `new_cluster_proposal`, `entity_merge`, `codex_reconciliation`, `decision_supersession`, `stale_work`, and `forget_request` (C11's /forget — self-describing item_content: query + matched turns/edges with excerpts); statuses pending/approved/rejected/resolved/**stale** (C10 flips items whose conversation was deleted — render stale as non-actionable history). All item_content is self-describing JSON by design; the panel is a dumb renderer + approve/reject buttons over `review.py`.
- [ ] **F3 Graph view of Codex** `(planned)` — Obsidian-style interactive graph; user can inspect, edit, and delete entities/edges manually. **Updated for the A7 model:** the backend is now literally a typed, bidirectional, rich-note graph, so the view should render `entity_type` (node colour/shape), the entity `description` note body (on click), **both** outgoing links and incoming backlinks, and **negated edges (A8)** distinctly (e.g. dashed/red — a stored fact, not a positive link). Edits must write to the right fields (manual text → `description`, not the auto-assembled `context_payload`) and trigger `_regenerate_context_payload`, matching how the inject-watcher now works. A7.4 community summaries, once built, become the zoom-out overlay.
- [ ] **F4 Full settings exposure** `(planned)` — Every knob editable in-UI: dynamic budget max, max input/output tokens, temp, top-p/k, thresholds — no file editing or raw API calls needed.
- [ ] **F5 Telemetry & forensics layer** `(planned)` — Expanded SSE event set (system has outgrown the current 5), real-time retrieval attribution (which leg, which bonuses, why), memory provenance (source_batch_id back-links), model thinking visibility (opt-in), and a subtle background-workers activity indicator. The Track-A codex work already emits new structured log events that are prime candidates to promote to SSE: `codex_grounding` (A2 over-rejection), `codex_reconcile` (A6 decisions), `codex_enumeration`, `codex_relation_overlap`, `grounded_query_expansion` — surfacing these gives real "why did retrieval do that" attribution. **B2 adds `memory_decision`** — the full log-odds breakdown (`p_ltm`, `p_len`, bumps, `p_need_mem`) behind the retrieve/no-retrieve call — the single most direct "why did (or didn't) memory fire" signal to surface.
- [ ] **F6 Select-text → add-to-context** `(planned)` — Select part of an answer and pin it as context for exactly the next prompt.
- [ ] **F7 Real-time search integration** `(planned — likely adapt existing tooling)` — Web search so the Real_Time_Search label actually routes somewhere.
- [ ] **F8 Deep research mode** `(planned — likely adapt existing tooling)` — Deep-research flow whose outputs are ingested through our own stores.
- [ ] **F9 Feedback UI** `(planned)` — Thumbs up/down + label-correction toggles (frontend half of B4).
- [ ] **F10 Conversation import (LSREP as migration tool)** `(new — flagship)` — Expose the replay pipeline as user-facing ingestion: drop exported ChatGPT/Claude/DeepSeek logs, system *lives through* them (post-flight, Codex, procedural, clustering, reflection) producing a mature memory state, not a searchable archive. Decay policy choices at import: preserve (decay_immune window), simulate natural decay (fast-forward), or start fresh. **Same philosophy powers E4's project cold-start bootstrap** (replaying a repo's git history as temporal memory). *(G23 shipped the OTHER import 2026-07-19 — the D2 state-copy (`scripts/ice_import.py`, id-preserving, manifest `kind: state-copy`); F10's replay ingestion re-lives history through the pipeline instead. The store is EMPTY at 1024 since the C17 wipe — F10 is its first real filler.)* **→ spec: [docs/specs/F10_F14_import.md](specs/F10_F14_import.md) (one ingestion engine shared with FINAL's adapters; ChatGPT/Claude/JSONL formats; decay-policy trio settled; hash-idempotent resume).**
- [ ] **F11 Cloud API models** `(planned)` — Let users register cloud API models alongside local ones (pairs with B1's cloud toggle; local-first stays the default posture).
- [ ] **F12 Multi-model responses** `(new — PARKED behind B3, 2026-07 audit)` — When a prompt is genuinely dual-natured (e.g. emotional + coding), consult two specialists and compose. **Why parked:** 2× latency + 2× VRAM for a rare prompt class, stacked on a router that is currently ≈neutral (B3 deferred for lack of true specialists). Only revisit after B3 ships *and* proves single-specialist routing helps — if one specialist doesn't beat the generalist, two won't.
- [ ] **F13 Session replay + conversation branching** `(planned — F2/F4 old table)` — `session_replays` table is written by nothing today; branching retrieval logic deferred until the frontend exists.
- [ ] **F14 Raw log extraction v2** `(rework)` — For unformatted text dumps: word-boundary slicing with an in-session overlap-resolution pass (both slices + overlap asked about in one open session), replacing the amnesia-method cold chunks; dedup at the end. **→ spec: [docs/specs/F10_F14_import.md](specs/F10_F14_import.md) (word-boundary slices + one open overlap session per seam; synthetic-time provenance marked for T-honesty).**
- [ ] **F15 Hardware advisor & model recommender** `(new — user, 2026-07)` — Onboarding-time (and on-demand) system scan → concrete model recommendations, in the spirit of the local-AI setups that probe specs and tell you what fits: detect **VRAM (per GPU), RAM, CPU, disk**, then recommend per ICE *role* — main generalist (size + quantization + **max context that actually fits**, KV-cache-aware: context length is VRAM too), background model (shared-by-default per the C7 decision; recommend dedicated only when real headroom exists), embedder, and — the MoE half — **a recommended specialist pool**: how many specialists at what size/quant fit alongside the generalist under Ollama's swap behavior, e.g. "24 GB: 14B-Q4 generalist + 2×7B-Q4 specialists (hot-swapped)" vs "12 GB: 8B generalist only — skip MoE." Feeds **B3** directly (a specialist pool sized to real hardware is the prerequisite for routing being worth anything — FINAL note 2's "so damn specialised they're actually helpful" starts with *fitting*), reads/writes the **model registry** (recommendations become registry entries on accept, with `context_window` set honestly per C16), and re-runs when the user adds a model ("will this fit? at what quant/context?"). Local-first: pure `nvidia-smi`/procfs probing, a static fit-table + headroom math — no cloud call, no telemetry.
- [ ] **F16 User retrieval overrides: force-deep-search + aggressiveness control** `(new — recovered from rough notes 2026-07-12; was in "Unified Context Budgeting with User Control" and never made it into any item)` — Two user-facing retrieval *behaviors* (not mere settings exposure, so F4 doesn't cover them): (a) a **"deep search" action** — one button/command that bypasses B2's gating for this query and deliberately runs the wide net over the full scope ("I know it's in there, look harder"); (b) a **retrieval-aggressiveness setting** per conversation/project (capped / dynamic / aggressive), feeding the B2 threshold and budget policy. Backend halves are small (a scope flag + a B2 bump/override + C16's budget policy hook); the surfaces ride F1/C11 (button + chat command — **the C11 parser is live since 2026-07-19**: a `/deep-search` command slots into `src/api/chat_commands.py`'s v1 set as one more ≤5-line handler; `/search` already forces retrieval, so (a) is mostly the wide-net/full-scope flag). Design against C16's need-based filling — aggressiveness is a *policy input* to it, not a competing mechanism.

## Track G — Ops, bugs & hardening (the "forgotten problems")

- [x] **G1 Classifier model-path mismatch** `(bug — critical)` — Done 2026-07 (with B4-promotion). `fine_tune.py` and `drop_zone.py` both hardcoded `v2_final.pt` while the live path is `v3_qwen_ft3.pt`, so fine-tune output went nowhere. Now both read `settings.classifier_model_path` (single source of truth), and `fine_tune.py` trains from *and* promotes onto that path — input, output, and inference path agree. Verified: no `v2_final` hardcodes remain in `src/`. The validated-promotion *step* (the B4 half) rides on this; its live run pends Z1.
- [ ] **G2 Background-model client cleanup** `(bug — RESCOPED 2026-07: original claim stale)` — Audit finding: the live `get_bg_client()` **does** respect `background_model_mode` (shared → Ollama, dedicated → :8002); the "hardcodes SGLang :8001 / dedicated commented out" description referred to the *experiment-era block that is itself the commented-out code* at the bottom of [bg_client_factory.py](../src/workers/bg_client_factory.py). Remaining scope updated by the shared-first decision (C7, 2026-07): delete the dead commented block, flip the default to `background_model_mode=shared` with bg model = the chat model, and remove vllm_bg from the default `./ice` stack (dedicated stays as power-user config — the seam survives, the service leaves the stack). Keep the `./ice` edit script-thin: `./ice` is itself end-state-doomed scaffolding (F preamble — replaced by the packaged app + E7's separate headless-MCP boot). Lands with C7's scheduling rework.
- [ ] **G3 Runtime shared↔dedicated switching** `(planned — PARKED by the shared-first decision, C7 2026-07)` — Switching background-model mode currently requires a full restart; make it hot-switchable. **Parked:** with shared as the only shipped default (dedicated = optional config for big rigs), hot-switching a mode most users never touch isn't worth building. Revisit only if dedicated usage turns out to be common.
- [ ] **G4 GPU gating fixes** `(bug)` — `GPU_UTIL_THRESHOLD = 20` is far too aggressive (LLM boxes idle at 10–30%) → workers starve and queues build; raise to ~60–75 and cache the nvidia-smi poll instead of spawning a subprocess per task. **Updated by the shared-first decision (C7, 2026-07):** in shared mode the meaningful gate isn't GPU utilization at all (the bg model IS the chat model) — it's "is a generation in flight / was the user just active": gate on Ollama `/api/ps` or a proxy-side in-flight flag + `is_user_active` (fixed in G21), and keep nvidia-smi gating only for the dedicated-mode path.
- [ ] **G5 SSE stream resiliency** `(bug)` — Partial/unclosed JSON chunks from Ollama are silently dropped in the stream parser → truncated `raw_text` stored → degraded extraction downstream. Guard the parse / use a partial-JSON parser.
- [ ] **G6 DB indexes via migrations** `(bug)` — `scripts/database/create_indexes.sql` isn't applied by Alembic; `batch_id` lookups full-scan episodic_memory at scale. Fold indexes into a migration. *(Down-payment 2026-07-12: Track T's migration `e5b8c2d4a917` added `ix_episodic_memory_timestamp`, `ix_cold_storage_timestamp`, and the two `codex_edges (source/target, valid_until)` indexes via Alembic — the remaining G6 work is the rest of the orphan SQL script, notably `batch_id`.)*
- [ ] **G7 Idempotency enforcement** `(bug)` — `episodic_memory.idempotency_key` has no unique constraint (informational only); enforce it.
- [ ] **G8 Sticky-state persistence** `(bug)` — `SESSION_STATE` is an in-process dict; model stickiness resets on restart. Back it with Redis.
- [ ] **G9 Constants → configuration** `(rework)` — Decay rates, RRF k, bonus multipliers, token budgets etc. are module-level constants; tuning requires code edits. Move the tunable ones into settings.
- [x] **G10 Compaction scheduling** `(bug — corrected 2026-07; merge half DONE with C5, compaction half DONE with C7 2026-07-11)` — Beautifully written, never ran: `compact_entities` wasn't beat-scheduled, so `codex_events` grew unbounded. **Settled by C7:** compaction runs on the maintenance runtime's 24h cadence (`maintenance_intervals["compaction"]`); the orphan audit is moot — beat no longer exists, and the runtime's JOBS table + intervals dict are one source of truth (a job without an interval is deliberately event/consent-only). `merge_similar_clusters` → `run_cluster_merge` every 3h (C5, carried into the runtime). **Track-T constraint honored:** compaction stays **lossless** (marks `compacted` + snapshots, never deletes — re-verified during the C7 port; the constraint is now noted in the callable's docstring).
- [ ] **G11 Batch-summariser coverage** `(bug)` — Only decayed turns are summarised; very old but undecayed turns in long conversations never compress.
- [ ] **G12 Dynamic LLM timeouts** `(bug)` — Hardcoded 30 s timeouts cascade into retry storms in shared mode; scale timeout with max_tokens / load.
- [ ] **G13 Drop-zone duplicate classifier** `(bug)` — `drop_zone.py` instantiates its own `PyTorchClassifier` instead of sharing the embedder — doubles memory during ingestion.
- [x] **G14 Memory-slot token budget enforcement** `(planned — F7 old table)` — **Done 2026-07-19, folded into C9 (`2c33258`) as specced:** `SLOT_TOKEN_CAP = 300` enforced in `services/slots.py::update_slot` (every write path — REST/MCP/review-approve — inherits it); hard-truncate + `truncated`/`warning` fields in the response, logged.
- [ ] **G15 Null_Noise / Casual_Banter routing** `(planned — F6 old table)` — Minor special-casing for noise labels.
- [x] **G16 None-scope isolation** `(planned — F11 old table)` — Done 2026-07 (with C6's backend primitive), **validated 13/13 live-DB** (`tests/test_session_scoping.py`). Semantics settled by user: **"store private + read nothing."** `episodic_memory.is_private` (migration `e8f3a26c5d17`, backfilled from current scopes): none-scoped turns are stored private — global retrieval carries an `is_private = FALSE` visibility invariant on every episodic query (bm25 main+fallback, vector, wide-net), an explicitly conversation-scoped query being the only door in; retrieval-side the incognito scope = `{conversation_id, isolated, incognito}` → episodic legs conv-only, codex resolves A5's `isolated` empty set, procedural + RAG legs skipped; write-side, post_flight skips codex/procedural extraction and clustering/batch-summary/reflection all exclude private turns (nothing leaks into shared stores). Scope changes via the user-control endpoint propagate the flag both directions. **Bonus leak fixed:** `_wide_net_fallback` previously ignored scope entirely (searched every conversation, unscoped codex, RAG always) — exactly when the classifier was least confident; it now honors the same visibility rules (the wide net widens *ranking*, not *visibility*).
- [ ] **G17 Audit trail** `(planned — F3 old table)` — Annotate every memory write with its source (user / post_flight / codex_extractor / reflection / manual_injection / bookmark — the sentinel is gone, D2); queryable + exportable. Feeds F5 forensics. **Vocabulary already live in CodexEvent payloads (partial):** `mcp_edit`/`manual_edit` since E0 (`services/graph.py::entity_edit`), `maintenance_agent` since D1 (every agent write carries `source` + `batch_source=agent_run_id`) — the remaining work is annotating the *other* writers and the query/export surface; the audit-trail column can backfill from events.
- [x] **G18 Celery observability** `(rework — FOLDED into C7's Celery-survival decision, 2026-07)` — Result backend was configured but unused. **Died with Celery (C7, 2026-07-11):** the maintenance runtime's `maintenance_ledger` table (last_started/finished/status/error/runs per job) + `maintenance_job_started/finished` structlog events are strictly better observability than the result backend ever offered — and they're the F5 telemetry source and D1 agent surface.
- [ ] **G19 Simulation-harness upkeep** `(planned — F8 old table)` — Add procedural extraction to the replay loop and log runs to a `simulation_runs` table for reproducibility. **Sync-risk warning (2026-07 audit):** [configurable_orchestrator.py](../src/retrieval/configurable_orchestrator.py) is an ablation-only subclass that mirrors parent internals — every orchestrator change (A4/A5/A7/A10/A11 all changed it) can silently desync the flags, making future ablations *lie*. Before the FINAL experiment redo: audit it against the current orchestrator, and prefer folding the feature flags into the parent (settings-driven) over maintaining a shadow subclass.
- [ ] **G20 Dead / half-dead code sweep** `(bug — 2026-07 audit, extended by the full-code review)` — Verified in code: (1) ~~duplicated model-selection block~~ **done with C16 (2026-07):** both duplicate selection blocks consolidated into one *pre-budget* selection, and a third stray `ollama_url = settings.ollama_base_url` line — which had silently disabled registry `base_url` routing for every request — deleted with them. (2) **`_hyde_used`** is always False (HyDE is commented out) — cosmetic flag threaded through logs. (3) **`conversations.custom_filter`** is *settable* via user_control but **never read by retrieval** — a half-dead feature: either wire it into scoping (C6) or drop it. (4) **`session_replays`** written by nothing (acknowledged; F13 owns its fate). (5) ~~`entropy_score`~~ **resolved by C1 (2026-07): implemented** as the real facts-per-token density score (turn_density.compute_entropy), written by post_flight. (6) **Redis `chat:completed` pub/sub** — published on every turn (with a fresh connection open/close per turn), **zero subscribers anywhere** — either it becomes F5's SSE feed (promote deliberately, with a shared connection) or delete the publish. (7) **`data/post_flight_buffer.jsonl`** — Celery-enqueue failures append to this file and *nothing ever replays it*: a safety net with a hole; add a startup replayer (C7's catch-up is the natural home) or delete the fallback. *(E0 update 2026-07-17: the extraction deliberately preserved the router surface byte-identically — parity was the gate — so items 2 (`_hyde_used`) and 3 (`custom_filter`, now threaded through `services/scoping.py` verbatim) were NOT swept; they belong to C6 (wire-or-drop custom_filter) and the HyDE relabel note in P0.1. Items 6/7 died with C7.)*
- [x] **G21 Audit hotfixes (fixed in-session, 2026-07)** `(bug — P0)` — Two live bugs found and fixed during the full-code audit: (1) **Celery worker could not boot** — `celery_app.py`'s `include` listed `src.workers.cluster_merge`, a module that **never existed** (the real task is `clustering.merge_similar_clusters`), so worker start died with `ModuleNotFoundError` and *every* background feature (post-flight, extraction, decay, clustering, reflection) was dead until restart-with-fix; removed the bogus entry — verified `import_default_modules()` now registers all 22 tasks. (2) **`gpu_check.is_user_active` was silently always-False** — redis `get()` returns *bytes*, `datetime.fromisoformat(bytes)` raises `TypeError`, and the bare `except: pass` swallowed it, so shared-mode workers **never yielded to an active user**; fixed with `decode_responses=True` + debug-log on failure — behaviorally verified (just-chatted → True, idle → False). Both bug classes (import-time breakage, silently-swallowed exception) are exactly what **G22's smoke suite** exists to catch.
- [x] **G22 Smoke-test suite** `(new — highest-ROI item in Track G; from the audit)` — **Done 2026-07-10 (Phase 0):** `tests/smoke/` (pytest + ruff added as dev deps): **import sweep over every module under `src/`** via filesystem glob (pkgutil silently fails on namespace packages — lesson recorded in the test; only `drop_zone` excluded, with its G13 reason; main.py included since its classifier loads lazily in lifespan), B2 decision math + C16 budget clamps + memory-pressure monotonicity, shared-chunker bounds/overlap/passthrough, `compileall` sweep. **48 green in ~35s; red-check verified** (an injected bogus import fails the sweep — the G21 class is now caught pre-commit). Habit: `uv run pytest tests/smoke -q` before every commit (recorded in the top flow). Remaining tail of the original scope — converting the critical standalone GPU+DB tests — happens incrementally as their subsystems get touched (boy-scout rule). Ruff isort config (`known-first-party=["src"]`) added to pyproject; main.py imports organized as the first boy-scout pass.
- [x] **G23 Data longevity: export / backup / embedding versioning** — **DONE 2026-07-19 (commits `78537c9` backup module, `d4d0a79` core; validated `tests/test_longevity.py` 26/26 over the spec's 8 checks + full regression sweep: c10_c11 56, c4_c9 28, services 48, mcp 21, timescope 61, coding_core 46, maint_agent 43, maint_runtime 49, reconcile 16, session_scoping 13, retrieval 3, smoke 77).** Shipped: (a) `src/memory/store_meta.py` — store-level embedding identity {model, dim, stamped_at} + per-table re-embed stamps; the fail-loud guard with ONE home (`create_core()` — both boot paths): settings↔stamp mismatch refuses boot naming `uv run alembic upgrade head` + `scripts/ice_reembed.py`; pending stamps boot with a loud warning; no-stamp bootstraps ONLY on provably-empty stores. (b) Portability `src/memory/portability.py` + `scripts/ice_export.py`/`ice_import.py` — JSONL-per-table state-copy walked from `Base.metadata.sorted_tables` (26 tables; `maintenance_ledger` excluded — runtime-lease hazard; vectors excluded by default; secrets never exported), staged import (alembic heads must MATCH, empty-or-`--merge`, self-ref FK backfill, force re-embed pass, codex payload sweep). (c) Backup `src/memory/backup.py` + `scripts/ice_backup.sh` — pg_dump -Fc (host-else-container) + models/ + .env + RESTORE.md + backup_info.json; `snapshot_db()` is FINAL's per-checkpoint API; `backups/` gitignored. (d) Re-embed runner `src/memory/reembed.py` + `scripts/ice_reembed.py` — vector columns catalog-introspected (never hardcoded), per-writer-VERIFIED source-text rules registry (episodic = the user half of raw_text; codex = canonical_name SKIPPING merge husks, C10 deletion husks, and non-conversation sources; centroids recomputed as normalized member means AFTER episodic, never encoded), kill-safe per-table stamp resume, up-front estimate, unregistered column = HARD ERROR naming the spec. Also consolidated the embedder: `src/memory/embedder.py::get_embedder()` — the FIVE separate `truncate_dim=384` SentenceTransformer copies (classifier, codex_extractor, procedural_extractor, batch_summarizer, clustering) became ONE shared native instance (G13 finally true; smoke suite 34s→13s). **USER-REQUIRED completed:** archive `backups/ice_backup_20260719_115426_paper-era-pre-1024-wipe.tar.gz` (17 MB: DB dump + models + .env) — plus the user's pre-existing Exp2 mature snapshot found at `~/ice_exp2_mature_snapshot.sql` (47 MB, 2026-06-29). Then, BY USER DECISION, the live store was **emptied instead of re-embedded** (the new experiments regenerate everything; the old data lives in the archives). Look-ahead honored: F gets buttons over these exact scripts (ledger); F10's replay import is the sibling of D2's state-copy (the manifest `kind` field names which is which); FINAL's snapshots call `backup.snapshot_db()`; C3/C12 re-chunks ride the same runner; T3 resurrection embeds at whatever store_meta says. **BRUTAL_ASSESSMENT's ⚡⚡ unbacked-up-live-DB + truncating-tests item is CLOSED** (backup shipped; `test_retrieval.py` — the one TRUNCATE-on-run test — rewritten house-style).
- [ ] **G24 Async hygiene in the hot path** `(bug-adjacent — from the audit)` — `chat_completions` is `async def` but issues several **synchronous** `db.query(...)` calls directly on the event loop (only some paths use `asyncio.to_thread`), blocking all concurrent request handling during DB waits. Single-user tolerable today; fix opportunistically when E0's service-layer extraction rewrites main.py anyway (wrap DB work or move to async sessions — decide once, in the service layer, not per-call).
- [ ] **G25 Log privacy** `(new — from the audit)` — Raw prompts and full responses go to plaintext logs (`logs/*.log`), in tension with the sovereign-memory posture: the DB is the *intended* store, logs are an unintended second copy that never decays and gets no scoping/deletion semantics. **C10 (2026-07-19) deliberately does NOT touch logs and says so:** every deletion manifest restates the caveat verbatim (`services/conversations.py::LOGS_CAVEAT` names G25 as the owner) — so this item is now user-visible in every deletion, not just an audit note. Add a settings toggle to redact/truncate message content in logs (keep IDs, decisions, and telemetry), defaulting to redacted once the F-track product era starts; update the caveat string when it ships.

- [x] **G26 chat_completions classifies before the conversation is resolved** `(bug — P0; found during T-spec grounding 2026-07-10)` — [main.py](../src/api/main.py):231 calls `classifier.classify(..., conversation_id=str(conversation_id))` (and the session-stickiness block reads it too) but `conversation_id` is first assigned at the "state tracking boundary" ~40 lines later (271–283): `conversation_id` is function-local, so as written **every request raises UnboundLocalError** — and even a non-crashing variant would classify without the conversation (CL7's context-prefix classification reads the last 3 turns, impossible before resolution). Likely a C16-reorder casualty. Fix: move the conversation-resolution + scope block above the classify call, keep classification before model selection (its tags feed routing); verify with a live request. Do as the first post-S1 hotfix. **Status 2026-07-10: code fixed (commit 8d52b15)** — block moved, compiles, import-sweep green. **Live round-trip DONE 2026-07-11** (first stack run, during C7 implementation): two-turn conversation through the proxy against Ollama `qwen3:4b-instruct` — (a) 200 + streamed SSE answer on both turns; (b) CL7 context prefix live on turn 2 (`cl7_context_prefix words=22` — log line added to [classifier.py](../src/classifier/classifier.py) per this entry's validation ask), classification saw the prior turn (p_ltm 0.979) and the model answered the memory probe ("what did I call my project" → "ICE") correctly. Checked off per earn-the-checkmark.

## Track H — Research follow-ups & open questions

- [ ] **H1 Cross-conversation retrieval evaluation** `(open)` — All 1,211 probes were within-conversation; the scoping/cross-convo machinery (and C6) has never been measured. **S1 note: FINAL's probe taxonomy includes a cross-conversation class (synthetic set has multi-conversation fact lifecycles) — re-verdict this after FINAL.**
- [ ] **H2 Multi-user evaluation** `(open)` — All benchmark conversations are one user's; generalisation across users is unvalidated. **S1 note: partially discharged by FINAL (synthetic persona = a second "user"; LongMemEval = third-party data) — full multi-user study stays future work.**
- [ ] **H3 Year-scale memory studies** `(open)` — 93 days max simulated so far. Saturation, retrieval drift, decay convergence/cold-start (does everything but bookmarks decay to zero?), compaction cadence.
- [ ] **H4 Probe realism** `(open)` — LLM-generated probes under-represent anaphoric/ambiguous human questions; grow the manually-authored probe set. **S1 note: FINAL's taxonomy reserves 15% for human-authored anaphoric/ambiguous probes — this is that growth.**
- [ ] **H5 Fine-tune scheduling on user machines** `(open)` — No good answer yet for when to run expensive fine-tunes for users who close the app (tail of C7). **S1 note: SETTLED by C7 D6 — consent-gated proposals (session-end + threshold + `auto_finetune` default OFF), never unattended, no cron.**

---

## SEMIFINAL — Full end-to-end system test (do SECOND LAST, after everything above)

- [ ] **Z1 Complete live system test** `(gate)` — **The very last thing, once every item above is done.** Bring up the *whole* stack (`./ice`: docker postgres+redis, Ollama, celery worker+beat, uvicorn proxy — bg model SHARED per the C7 decision; vLLM only if testing the optional dedicated config; `./ice` is still the dev harness at Z1 time — the packaged app and E7's headless-MCP boot replace it *after* this gate, per the F end-state) and drive a real end-to-end run through our frontend / the API: real chat turns → classify → retrieve → assemble → route → stream → post-flight → codex/procedural extraction → reconciliation → enrichment → clustering → decay → reflection. Verify the pieces that could only be *stubbed/DB-validated* during development actually work against the live background model — especially the LLM-dependent ones flagged "pending live validation": A2 extraction quality + over-rejection rate, A6 reconciliation decisions, A7.3 note enrichment (fills the ~1,150-entity backlog), plus MoE routing, summaries, and the full worker cadence. Measure, not just run: re-check the Codex contribution and retrieval quality now that A1–A7/A10 landed. This is the acceptance gate for the whole post-paper cycle. **Expanded (user, 2026-07-10): Z1 is also the tuning + coverage gate — "test every single thing" is encoded here, and FINAL stays strictly after it.** (a) **Whole-system coverage matrix:** enumerate every subsystem/module × which roadmap item touched it this cycle; anything untouched gets an explicit review pass — nothing ships unexamined just because no item happened to rework it. (b) **Staged parameter-tuning pass:** tunable constants live everywhere (leg weights, RRF k, bonuses, thresholds, taus, budget formulas, B2 log-odds weights, T-track pads) and exhaustive combination testing is impossible — so Z1 runs the Z1-prep spec's protocol: consolidate knobs into settings (G9), then tune stage-by-stage along the pipeline in dependency order (candidate generation → fusion → bonuses → budget → decision thresholds), one-factor sweeps per stage against the synthetic ledger auto-scorer (FINAL's machinery, built early and reused here), record a sensitivity verdict per knob (load-bearing / plateau / cosmetic), and freeze the tuned defaults BEFORE FINAL measures the system. **→ spec: [docs/specs/Z1_tuning_coverage.md](specs/Z1_tuning_coverage.md) (greedy stage-wise descent + jitter pass; dynamics solved by invariant math, not sweeps; coverage matrix + pending-validation ledger as Z1's entry checklist; G9 lands as its first commit).**


## FINAL — Redo the entire experiments

> **📄 Publishing path for the resulting paper (and paper #1): [PUBLISHING.md](PUBLISHING.md)** — arXiv → TMLR (journal, no presentation ever, no page limit, free), endorsement routes, scam warning, and the paper-#2 lessons (CIs from day one, pre-registered deletion rules, tag-before-running).
>
> **⚠ THE FLOW WILL CHANGE (user, 2026-07-12).** Even with everything thought through below and in the spec, expect the *entire* experiment flow to be revised at execution time — additions, subtractions, edits. Concretely, every stage of how the previous experiments were done is up for change: sourcing/getting the conversations, numbering + timing them, generating the probes, building their ground truth, choosing which conversations get tested, the testing process itself, and how each part is split into its own script. The spec is the starting design, not a contract — treat the frozen `experiments/*` folders as the record of what WAS done, never the template for what will be done. (Long-form reasoning: [BRUTAL_ASSESSMENT.md](BRUTAL_ASSESSMENT.md) §4.)
>
> → spec: [docs/specs/FINAL_experiments.md](specs/FINAL_experiments.md) (S1, 2026-07-10 — decision-complete. User forks settled (cloud lane re-settled same day on cost-comfort: **one flat ~$20 Ollama Cloud paid month is the plan of record** — the calibration-gated judge keeps rigor measurable, the frontier baseline IS the "$20/month product" criticism #1 names; Claude API demoted to an optional ~$5–10 prestige appendix, Pro stays the coding lane; **new condition `full_ice_cloud`** = ICE + the same cloud answerer, the equal-answerer memory head-to-head); a **synthetic publishable dataset** with a planted-fact ledger (reviewers can't replicate private sets — and ledger GT kills the GT-error class); **LongMemEval** external anchor; **per-condition modular resumable runs** (no mega-script; laptop-intermittent); budget-parity conditions; per-dataset stats only; MoE runs behind a pilot gate; judge calibrated against the human-audited 1,211 + ledger auto-scores.)

- [ ] **IMPORTANT** - 
1. This is important as this isnt just a coding thing, it has a redesign of the exp so we dont run in to problems and limitations like before paper.
2. We need better moe model that as so damn specialised that they being called is actually helpfull.[this might depend upon the classifer too as if it just gives it all to general or a selected model it will be bad and also the diversification of the test convos]
3. A few things that were additionally wrong last time-

```
1. The "Straw Man" BaselineThe author compares ICE to a vanilla, single-leg Vector RAG baseline. They justify not comparing it to commercial systems (like ChatGPT's memory or Claude's projects) by stating those cannot be run locally with controlled retrieval.  Why it's a flaw: This is an easy way out. While you can't control Claude's internal RAG, you can easily run the LSREP benchmark queries through Claude or OpenAI APIs to see if building this massive, self-hosted system actually yields better answers than just paying $20 a month for an off-the-shelf product. By only comparing ICE to a basic vector database, they set a very low bar for victory.
2. Terrible ROI on ComplexityThe ICE architecture is staggeringly complex. It requires a FastAPI service, a PostgreSQL database with pgvector, a Redis/Celery fleet to manage 14 different background workers, and multiple locally hosted language models.  Why it's a flaw: Look at the main evaluation (excluding the one dataset where the baseline crashed): ICE scored a 4.26 while the baseline scored a 4.25. Building an enterprise-grade microservice architecture to get a 0.01 bump in answer quality for a single user is massive over-engineering. The author largely ignores the extreme operational overhead, hardware requirements, and maintenance burden this puts on anyone trying to use it.  
3. The Token Efficiency Claim is MisleadingThe paper heavily pushes the narrative that "less-but-better context outperforms more-but-noisier context" and highlights that ICE injects 32% fewer fragments than the baseline.  Why it's a flaw: If you look closely at Table 6, ICE actually consumes more total tokens (22,411) than the vector baseline (21,025) on the conversations where both systems successfully generate answers. ICE retrieves fewer pieces of information, but those pieces are much longer. The claim that it's highly token-efficient is functionally untrue unless you specifically look at the one stress-test conversation where the baseline system broke completely.  
4. A Half-Baked Ablation StudyThe ablation study (Experiment 3) is meant to prove which specific features of the system actually help and which are useless.  Why it's a flaw: They only ran this study on one of the four datasets (Dataset B, the fantasy world-building one) and they used a weaker 14B model instead of the 26B model used in the main tests. They proudly note that their dynamic token budget actually hurt performance in this test, but they never ran the ablation on the technical dataset where the budget was theoretically the hero. You cannot make sweeping claims about architectural features by only testing them on a dataset where you know half of them don't apply.  
```

4. Be extremely brutal and puch holes in the whole evaluation so we can actually measure all of the systems capabilities, and think how the hell we will do this.
5. **The time axis was never measured (Track T, 2026-07-10):** all 1,211 probes ask for the current truth — none ask as-of ("what did I think about X in <period>"), range ("my ideas on X from H1-2025"), evolution ("how did X change over time"), or which-is-current questions. The saga-ending divergence (vector-RAG answered an old draft, ICE the new one, neither the evolution) is the canonical example. FINAL must include a temporal probe class scored on era-correctness and evolution narration, or the exocortex capability rots unmeasured again.